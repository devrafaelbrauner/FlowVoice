# Tarefas pendentes

Backlog do que falta para o FlowVoice ficar completo. O detalhamento por fase
continua em [`docs/tasks/`](docs/tasks/README.md); aqui fica a visão priorizada.
Itens concluídos são marcados, não apagados.

**Status:** pendente · em andamento · bloqueado (depende do usuário) · concluída

## Alta

| ID | Tarefa | Status | Origem |
| --- | --- | --- | --- |
| P01 | Commitar e publicar F04–F12 (um commit por fase, PR #2) | concluída (0.2.0) | checkout local sem commit |
| P02 | `commitText` com `newCursorPosition = 0` deixava o cursor antes do texto | concluída (0.3.0) | /verify |
| P03 | `FlowVoiceAdbReceiver` exportado sem permissão | concluída (0.3.0) | /verify |
| P04 | `allowBackup` restaurava `flowvoice_secrets` sem a chave do Keystore | concluída (0.3.0) | revisão |
| P05 | Ditado pelo botão flutuante sem sair do app-alvo | concluída (0.3.0) | revisão de F12 |
| P06 | Backend de sync real (Supabase Auth + Postgres com RLS) no lugar de `InMemoryRemoteSync` | bloqueado: precisa do projeto Supabase (URL e chave anon) | F11 |
| P07 | Login validado no backend (ID token do Google trocado por sessão Supabase); hoje o `id` do usuário é o e-mail | bloqueado: depende de P06 e do Web Client ID do Google Cloud | F10 |
| P08 | Decidir a licença: `AGENTS.md` propõe MIT, mas os demais projetos do autor são proprietários | bloqueado: decisão do usuário | F00 / F14 |
| P20 | Validar no S26 com fala real: botão flutuante → texto inserido no app-alvo, com dois ditados seguidos na ordem certa | validada no S26 (2026-09-14, 0.4.5): dois ditados seguidos pela bolha entraram na ordem no Samsung Notes (19:45:48 e 19:46:05, `commitText executado`, `inserted=true`); o 3º teve erro de reconhecimento (P131/P132) | F12.4 |
| P21 | Corrida no finalize: a última janela chegava à fila depois do `awaitIdle` | concluída (0.3.0) | F13 / F12.4 |

## Média

| ID | Tarefa | Status | Origem |
| --- | --- | --- | --- |
| P09 | `ModelInfo.contextLength` sempre nulo | concluída (0.3.0) | /verify |
| P10 | Rodar `lintDebug` no CI | concluída (0.3.0) | /verify |
| P11 | F13 — preparação para Windows (Compose Desktop, JNA `SendInput`, DPAPI) | concluída (0.3.0), exceto F13.7 | PLAN |
| P12 | F13.7 — validar num Windows real: inserção (emoji, acentos, UIPI), DPAPI, microfone, `packageMsi` | pendente: precisa de um Windows | F13 |
| P13 | F14 — higiene de licença e entrega: `LICENSE`, notices finais, documentação de release | pendente (depende de P08) | PLAN |
| P14 | Persistência em SQLDelight/SQLite, como decidido no PLAN (hoje notas e dicionário são JSON em SharedPreferences) | pendente | PLAN |
| P15 | Evidência em aparelho real de F06 a F11 (os docs dessas fases não registram validação no S26 Ultra) | pendente | docs/tasks |
| P16 | Docs desatualizados (AGENTS, F01, F02, PLAN) | concluída (0.3.0) | /verify |
| P17 | `README.md` com instalação, configuração e build | concluída (0.3.0) | revisão |
| P22 | Desktop: atalho global e janela flutuante que não rouba o foco (hoje minimiza e insere após 3 s) | pendente | F13 |
| P23 | Definir `upgradeUuid` e `menuGroup` do MSI antes do primeiro instalador | pendente | F13 |
| P24 | Onboarding: tratar recusa de `POST_NOTIFICATIONS` (a notificação do serviço de microfone some) | pendente | F12.4 |
| P25 | `TYPE_APPLICATION_OVERLAY` exige API 26 com minSdk 24: proteger por versão ou subir o minSdk | concluída (0.3.1, com P37): sem overlay e sem crash abaixo do Android 8; o aviso `InlinedApi` do lint continua | lint |
| P26 | `stopService` com instância nova em `MainActivity` (`ImplicitSamInstance` no lint) pode não parar o overlay | pendente | lint |

## Baixa

| ID | Tarefa | Status | Origem |
| --- | --- | --- | --- |
| P18 | Regressão do F02 em apps-alvo adicionais (WhatsApp, Chrome, Obsidian) | pendente | PLAN |
| P19 | Ícone do app (`MissingApplicationIcon` no lint) | pendente | lint |
| P27 | Botão flutuante arrastável, com posição lembrada (hoje cobre a tecla de ação do teclado) | concluída (0.5.0, via P138) | F12.4 |
| P28 | `GoogleSignInHelper`: tratar `NoCredentialException` e declarar `<queries>` para `resolveActivity` | pendente | lint |
| P29 | Levar dicionário, revisão e notas ao desktop | pendente | F13 |

## Achados da verificação de `9d527d7` (2026-09-13)

| ID | Prioridade | Tarefa | Status | Encaminhar |
| --- | --- | --- | --- | --- |
| P30 | Bloqueante | O fallback `ACTION_SET_TEXT` substituía todo o conteúdo do campo do app-alvo pelo texto ditado | concluída (0.3.1): insere no cursor; recusa em senha, texto ilegível e Android < 8 | /corrigir |
| P31 | Alta | Trecho com falha sumia do texto e a inserção parcial saía como sucesso, sem aviso | concluída (0.3.1) | /corrigir |
| P32 | Média | Corrida no início do ditado: toque duplo, cancelar e ocultar durante o `start` | concluída (0.3.1) | /corrigir |
| P33 | Média | A inserção não conferia o destino: ditado pela `MainActivity` gravava nos campos do próprio app | concluída (0.3.1) | /corrigir |
| P34 | Média | F12.4 marcada `[x]` sem aceite ponta a ponta | concluída (0.3.1): volta para `[~]` até P20 | /corrigir |
| P35 | Média | Testes do pipeline sem sessões seguidas, start duplo, falha de trecho, inserção falha, erro de captura e frames de outra thread | concluída (0.3.1) | /corrigir |
| P36 | Média | `EncryptedSecretStore`: com a chave-mestra inutilizável o app caía em loop ao abrir (`InstanceCreationException`); uma falha passageira do Keystore apagava a chave. Diagnóstico /debugar: confirmado (hipóteses 1 e 2a; 2b refutada) | concluída (0.3.1): `SecretVaultOpener`; a checagem da chave-mestra no Keystore não tem teste automatizado | /corrigir |
| P37 | Média | Overlay: `addView` sem proteção lançava `BadTokenException` sem permissão de sobreposição (ou na API 24–25), e `onDestroy` removia view não anexada. Diagnóstico /debugar: hipótese do `stopSelf` refutada, `addView` confirmado | concluída (0.3.1): `OverlayStartGuard`; caminho de exceção do `addView` só com teste manual (F12.md) | /corrigir |
| P38 | Baixa | Finalize cancelado continua: com a revisão ligada, o texto vai à OpenRouter mesmo depois de cancelar | pendente | /corrigir |
| P39 | Baixa | `POST_NOTIFICATIONS` só é pedida quando falta o microfone; complementa P24 | pendente | /corrigir |
| P40 | Baixa | `startedHere` nunca volta a `false` no overlay: ocultar o botão cancela um ditado iniciado pela `MainActivity` | pendente | /corrigir |
| P41 | Baixa | A captura Android parava sozinha quando `read` devolvia ≤ 0 e a sessão seguia "● Gravando" | concluída (0.3.1, junto com P47) | /corrigir |
| P42 | Baixa | Privacidade dos logs: o diagnóstico debug loga 40 caracteres do campo de outro app, `onStartCommand` de debug no código principal e o release loga o pacote de cada app ditado | auditado: dividido em SEG-3 e SEG-4 | /corrigir |
| P43 | Baixa | Docs: o `AGENTS.md` não cita `desktopApp` nos comandos nem no CI; o `PLAN.md` e o `requirements.md` ainda dizem que a implementação Windows está "adiada" | pendente | /corrigir |
| P44 | Baixa | Branches auxiliares no GitHub já integradas na `f03-audio-capture-session` | pendente: apagar após o merge do PR #2 | usuário |
| P45 | Alta | O runtime empacotado do desktop não tinha `java.net.http` (motor Ktor Java) | concluída (0.3.1); o CI confere o `release` do runtime | /corrigir |
| P46 | Média | Desktop: iniciar ou cancelar durante a transcrição final apagava ou misturava o texto de outra sessão | concluída (0.3.1) | /corrigir |
| P47 | Média | Captura desktop que falhava no meio só ia para o stderr; `read` = 0 com a linha aberta girava sem pausa | concluída (0.3.1) | /corrigir |
| P48 | Média (suspeito) | `SendInput` não sinaliza bloqueio por UIPI no retorno; retorno parcial não avisa e repetir duplica. Validar em F13.7 | pendente | /debugar |
| P49 | Baixa | Desktop: "Inserir" duplicado com duplo clique durante a contagem; "Cancelar" em `Finalizing` liberava o texto | parcial (0.3.1): o cancelamento na finalização foi coberto por P46; falta travar a contagem de inserção | /corrigir |
| P50 | Baixa | `THIRD_PARTY_NOTICES.md`: Skia é BSD-3-Clause, o Skiko embute HarfBuzz/FreeType/ICU e o MSI embute um runtime OpenJDK (GPLv2 + Classpath Exception) | pendente (F14) | /corrigir |
| P51 | Baixa | Nenhum teste exercita a chamada JNA real nem o tamanho de `WinUser.INPUT` (40 bytes no x64) | pendente | /corrigir |
| P52 | Baixa | Quebra de linha vira `VK_RETURN` (envia a mensagem em chats) e Tab vai como `VK_PACKET`; `mergeAdjacent` troca quebras por espaço com 2+ trechos | pendente: decidir a política | /aprimorar |
| P53 | Baixa | Desktop: o controller mistura thread da UI e `Dispatchers.Default` sem sincronização (`jobs`, `cancelled`, contador) | pendente | /corrigir |
| P54 | Baixa | `ProtectedFileSecretStore.readOpenRouterKey` não captura `LinkageError` (JNA) e decifra a chave a cada janela | pendente | /corrigir |
| P55 | Baixa | Empacotar no macOS falha: `jpackage` recusa `app-version` 0.x e o plugin barra o JDK do Homebrew | pendente | usuário |
| P56 | Baixa | Depois de P30, o fallback recusa no Android 7 (sem `isShowingHintText`) e em campos que reportam texto nulo: nesses casos não há inserção (antes apagava o campo) | pendente: avaliar se algum app-alvo real cai nisso | /debugar |
| P57 | Média | Tink 1.5.0 pode gravar o keyset **em claro** quando não consegue gerar a chave-mestra (`readOrGenerateNewMasterKey`), sem avisar | confirmado pelo /seguranca (SEG-6, rebaixado a Baixa: exige falha rara do Keystore e acesso ao sandbox) | /corrigir |

## Achados da auditoria /seguranca de `9722166` (2026-09-13)

Veredito: com ressalvas (0 críticos, 0 altos). gitleaks (histórico), semgrep (`p/kotlin`, `p/secrets`, `p/owasp-top-ten`), OSV API e lint Security sem achados confirmados.

| ID | Severidade | Tarefa | Status | Encaminhar |
| --- | --- | --- | --- | --- |
| SEG-1 | Média | Notas, dicionário e e-mail em SharedPreferences sem cifra entravam no backup em nuvem e na transferência entre aparelhos | concluída (0.4.1): excluídos do backup (`BackupRulesTest`); cifrar em repouso segue com P14 | /corrigir |
| SEG-2 | Média | Repositório **público**: a `main` ainda tem `FlowVoiceAdbReceiver` exportado sem permissão, e havia 2 artefatos `flowvoice-debug` da `main` publicados pelo CI | parcial (0.4.1): artefatos apagados; falta o merge do PR #2, autorizado, depois do /verificar da 0.4.1 | usuário |
| SEG-3 | Baixa | O release logava no logcat o pacote de cada app em que se dita; parte do P42 | concluída (0.4.1) | /corrigir |
| SEG-4 | Baixa | Caminho de debug do `onStartCommand` no `main`; `diagnoseFocusedField` devolvia conteúdo do campo sem checar `isPassword`; parte do P42 | concluída (0.4.1): `FocusedFieldDiagnostic` | /corrigir |
| SEG-5 | Baixa | `typeAllMask` no serviço de acessibilidade com `onAccessibilityEvent` vazio | concluída (0.4.1): nenhum evento assinado; validar fallback e barra no aparelho (P65) | /corrigir |
| SEG-6 | Baixa | Tink 1.5.0 gravava e aceitava keyset em claro quando o Keystore falhava (P57) | concluída (0.4.1): Keystore direto + migração; validar a migração no S26 (P65) | /corrigir |
| SEG-7 | Baixa | A rota direta não recusava campo de senha, e o destino não era conferido entre iniciar e finalizar | concluída (0.4.1): `InsertionGuard` + `captureTarget`; validar no aparelho (P65) | /corrigir |
| SEG-8 | Info | `SyncModels.kt:15`: a trava `contains("sk-")` derruba o sync com termo legítimo e não cobre notas | pendente | /aprimorar |
| SEG-9 | Info | Nenhuma tela usa `filterTouchesWhenObscured` (tapjacking); avaliar nas telas de chave e permissões | pendente | /aprimorar |

## Redesign 0.4.0 (2026-09-13)

| ID | Prioridade | Tarefa | Status | Encaminhar |
| --- | --- | --- | --- | --- |
| P58 | Média | Conferir no S26 o visual das telas e da barra de ditado contra as capturas do handoff (`~/Downloads/design_handoff_flowvoice_app/screenshots`), incluindo a barra ancorada acima do teclado e o tema claro | pendente: o aparelho estava em uso na entrega | usuário / /verificar |
| P59 | Baixa | Estatísticas do Início (latência média, ditados hoje, gasto hoje) e latência p50/p95 do Diagnóstico sem fonte persistida: aparecem como "—" | pendente | /construir |
| P60 | Baixa | O dicionário não guarda a forma ouvida nem o contexto ("brauner → Brauner"); os termos pendentes ficam só em memória e somem ao reiniciar | pendente | /construir |
| P61 | Baixa | "sincronizado" por nota e "Limite de gasto" do design não têm funcionalidade por trás; omitidos | pendente (depende de P06) | /construir |
| P62 | Baixa | Logo do Google no login e ícone do app ainda são placeholders (ver P19) | pendente | usuário |
| P63 | Baixa | `flagRetrieveInteractiveWindows` (para ancorar a barra acima do teclado) amplia o que o serviço de acessibilidade pode ler; revisar junto com SEG-5 | pendente | /seguranca |

## Correções de segurança 0.4.1 (2026-09-13)

| ID | Prioridade | Tarefa | Status | Encaminhar |
| --- | --- | --- | --- | --- |
| — | — | **Status dos achados do /verificar da 0.4.1 (2026-09-13):** concluídos na 0.4.2 — P67, P91, P92, P90, P80, P83, P81, P69, P66 e P84. P82 (fallback com texto antigo) provavelmente resolvido pela P81, validar no aparelho. Restam P68, P70–P79, P85–P89 | — | — |
| P93 | Média | Se a bolha do overlay assumir um ditado de nota (P91), "Inserir" leva o texto ao campo ativo **e** à nota; o mesmo pode ocorrer com o `finalize()` das Notas quando o foco está em outro app. Separar o destino por sessão | concluída (0.4.3, via P99/P100): destino por sessão (`DictationTarget`) | /corrigir |
| P94 | Média | Validar no S26 a 0.4.2 e a 0.4.3: barra ao ditar pelo Início (P91), ditado de nota com troca de aba (P67), recusa e nova tentativa (P80/P83), sessão longa parando no teto (P92), ícone abrindo o app depois das Configurações (P90), orientação de configurações restritas (P66) e TalkBack (P69); da 0.4.3: ditado de nota parando no teto sem digitar em outro app (P99), bolha assumindo ditado de nota com Parar (P100), nova tentativa depois do teto (P101), rotação nas Notas (P102), ocultar o botão com ditado de nota (P103), indicador de microfone apagado sem chave (P107), conferência de destino com o foco fora de um campo no Android 16 (P108) e `uiautomator dump` + TalkBack nos campos (P109); da 0.4.4: segunda tentativa sem chave nas Notas e no Início, com mensagem e sem nota armada (P110); da 0.4.5: ditado de nota com o app em segundo plano e a bolha desligada, conferindo `transcription_silent_window` no logcat e nenhum texto novo na nota (P121/P124), e fala baixa ainda transcrita, sem ser tomada por silêncio (P124) | parcial (2026-09-14, /verificar 0.4.2 no S26): P90 ok — "Exportar relatório" abriu o compartilhamento em tarefa própria (`#3049` intentresolver; FlowVoice `#3048` `sz=1`) e reabrir pelo launcher trouxe o FlowVoice; P69 ok — toggles "Botão flutuante" e "Revisão por IA" expostos com nome; Ajustes mostra a chave migrada (`sk-or-v1-••••46a7`, CONFIGURADA). Faltam P91, P67, P80/P83, P92 e P66 (exigem microfone ou serviço desligado), P81/P82 (barra acompanhando o teclado e fallback sem texto antigo) e o voltar das Configurações quando já há uma tarefa de Configurações nos recentes (P90); **0.4.4 no S26 (2026-09-14, reinstalada: a 0.4.3 tinha outra chave de debug)**: P110 ok sem chave, duas tentativas seguidas em cada entrada — Início (aviso "Não foi possível iniciar o ditado: chave OpenRouter ausente ou inválida" nas duas, sem "O microfone não respondeu a tempo."), bolha (barra "Falha no ditado" e liberação em 4 s nas duas) e nota (mensagem de chave nas duas; `dictation_failed stage=key` a cada tentativa). Achado novo P123; **0.4.5 no S26 (2026-09-14, `adb install -r` sobre a 0.4.4, chave preservada)**: P92/P99 ok — ditado de nota com a bolha desligada e o launcher em foco parou no teto (`dictation_budget_stop target=Note` na janela 30, `audio capture stopped`), nada digitado no launcher, nota vazia (30 janelas de ruído de sala sem texto inventado) e, numa segunda sessão, "Nenhum trecho transcrito (teto de requisições da sessão)" nas Notas; P67/P102 ok — com ditado de nota, Início → Notas e paisagem → retrato mantêm "Ouvindo… fale para continuar" e o botão de parar, que finalizou (`proofreading_applied`, `dictation_finalized chars=68 inserted=false`) e anexou o texto à nota; P124 não exercitada: em segundo plano a captura seguiu com `silenced:false` (P121). Achados novos P128 e P129. Método: cada `uiautomator dump` desconecta e reconecta o serviço de acessibilidade e, durante o ditado, devolveu tela velha; conferir a tela por `screencap`. Faltam, com fala real: P124 (fala baixa), P20/P91, P80/P83/P113, P114 e P81/P82 | usuário / /verificar |
| P99 | Bloqueante | Texto de nota digitado em outro app sem ação do usuário (P93 confirmado e ampliado pela P92): a sessão de nota começa sem revisão (`NotesRoute.kt:100`, `requestStart()`); ao atingir o teto, `stopAtRequestBudget` chama `requestFinalize()` (`DictationPipeline.kt:158-161`), que insere com `inserter.insert(text)` (`:301`) sem recapturar o destino (só `finalizeForReview` recaptura, `:199`). Com o usuário numa conversa do WhatsApp, o texto da nota (uso clínico) entra lá e também na nota. O teste `reachingRequestBudgetStopsCaptureAndCompletesWithWarning` fixa a inserção. Correção: sessão de nota nunca chama `inserter.insert`, e a bolha não mostra "Inserir" para ela | concluída (0.4.3, `584b187`); validar no S26 (P94) | /corrigir |
| P100 | Alta | Ditado de nota adotado pela bolha pode travar em `Ready` e perder o texto: o `NoteDictationCoordinator` ignora `Ready`, as Notas mostram "Ouvindo…" com "Parar" que não age fora de `Recording`, "Apagar" fica desabilitado e "Ocultar botão" não cancela (a adoção não liga `startedHere`). Só restam Cancelar (texto some) ou inserir em outro app (P99) | concluída (0.4.3, `6d7ee04`); validar no S26 (P94) | /corrigir |
| P101 | Média | Teto (P92) + conferência de destino (P83): numa sessão do Início, a parada automática captura o app em foco naquele instante (launcher ou app de passagem); a inserção no destino real fica recusada para sempre (tentar de novo não recaptura) e a barra mostra só 3 linhas, sem copiar: resta Cancelar e perder ~2 min de texto. Com o próprio FlowVoice em foco nas duas capturas, a conferência fica desligada | concluída (0.4.3, `526ce09`); validar no S26 (P94) | /corrigir |
| P102 | Média | P67 parcial na rotação: `NotesRoute` seleciona `initialNoteId ?: activeNoteId` e `noteToOpen` é `rememberSaveable`; abrir a nota A pelo Início, ditar em B e girar seleciona A, sem "Parar" visível nem marca de qual nota dita, e "Ditar" em A diz "Já há um ditado em andamento" | concluída (0.4.3, `e1f2f78`) | /corrigir |
| P103 | Baixa | "Ocultar botão" cancela ditado de nota: `startedHere` nunca volta a `false` (P40), e depois de uma sessão pela bolha qualquer sessão ocupada é cancelada, inclusive a de nota que agora vive fora da tela; o texto se perde | concluída (0.4.3, `da5cf0d`) | /corrigir |
| P104 | Baixa | P80: nova tentativa com a mesma recusa não dá retorno visual (`Ready` igual não emite no `StateFlow`); "apagar a nota em ditado cancela a sessão" não é alcançável (botão desabilitado) | pendente | /corrigir |
| P105 | Baixa | Testes: `OverlayOwnershipTest` só testa o redutor (a leitura de `pipeline.status.value` no efeito e o toque de adoção sem teste); `NotesScreenStateTest` passa por `onPipelineStatus`, que a produção não chama mais; `activeDictationIsVisibleToANewObserverAfterTheScreenIsRecreated` não recria nada | pendente | /aprimorar |
| P106 | Baixa (suspeito) | O teto conta no processamento, não no envio: com latência acima de 4 s a fila cresce e a captura passa das 30 janelas; janelas que falham antes do envio (chave ausente) não contam, então sem chave o microfone fica aberto indefinidamente (mesmo sintoma do P92). `focusedPackage()` pode ler `currentInputEditorInfo` antigo antes de limpar o cache | diagnosticado e corrigido via P107 e P108 (0.4.3) | /debugar |
| P107 | Alta | P106a confirmado por teste (/debugar, `P106ReproTest` descartável em `24a4d83`): sem chave OpenRouter, `IncrementalTranscriptionController.kt:94-98` falha a janela com `InvalidKey` e retorna antes de `requestCount++` (`:99`); o teto nunca é atingido e o pipeline só encerra por `budgetExhausted` (`DictationPipeline.kt:158-161`), então **sem chave o microfone fica aberto indefinidamente** (teto 2, 10 janelas: `budgetExhausted=false requisicoes=0 falhas=10`). Com latência acima de 4 s, a contagem em `process()` (serial sob `processMutex`) deixa a captura passar do teto (teto 2: 6 janelas capturadas ao esgotar). Correção proposta: contar na submissão e encerrar a sessão em chave ausente/inválida (`Failed`), recusando `start()` sem chave | concluída (0.4.3, `7e7f13c`); validar no S26 que o indicador de microfone não acende sem chave (P94) | /corrigir |
| P108 | Média | P106b confirmado no AOSP (android14-release): `InputMethod.mInputEditorInfo` só é zerado em `doFinishInput`, e o `InputMethodManagerService` devolve `NULL_EDITOR_INFO` sem finalizar o input quando a janela focada não tem editor; `focusedPackage()` (`FlowVoiceAccessibilityService.kt:73-77`) usa esse `EditorInfo` antes da janela ativa, então `captureTarget`/conferência de destino podem comparar com o último editor (recusa falsa ou aceite de destino fora de foco). Correção proposta: janela ativa como fonte principal; `EditorInfo` só com input iniciado e pacote igual; política pura `FocusedPackage.resolve` com teste. Não validado no Android 16 | concluída (0.4.3, `939b62c`) | /corrigir |
| P109 | Média | P95 parcialmente confirmado: `uiautomator dump` no S26 mostra `EditText text='Sem título' desc='Título da nota'` e `EditText text='' desc='Corpo da nota'`; o Compose 1.8.2 não expõe hint (só `setContentDescription`/`setText`), então a descrição fica ativa com texto, padrão proibido pela ajuda de acessibilidade do Android (`EditableContentDescCheck`). Efeito na fala do TalkBack não medido. Em Ajustes, os `EditText` da chave e do Client ID saem **sem nome nenhum** (o `label` do `FvDarkField` fica fora do nó do campo). Correção proposta (Opção A): `contentDescription` só com o campo vazio, aplicado no próprio `BasicTextField`, em Notas, Dicionário e `FvDarkField` | concluída (0.4.3, `9f50182`); conferir com `uiautomator dump` e TalkBack (P94) | /corrigir |
| P95 | Média (suspeito) | P69: `contentDescription` em campos editáveis (`NotesRoute.kt:460` título, `:494` corpo, `DictionaryRoute.kt:229`, `SettingsControls.kt:58`) pode fazer o TalkBack ler o nome no lugar do conteúdo (padrão acusado pelo `EditableContentDescCheck`): ao focar o corpo de uma nota ditada, ouve-se "Corpo da nota" e não o texto. Conferir com TalkBack; alternativa: rótulo visível num contêiner que agrupe o campo | diagnosticado e corrigido via P109 (0.4.3) | /debugar (TalkBack) → /corrigir |
| P96 | Baixa | P69: nomes duplicados no TalkBack. `SettingsRow` com `mergeDescendants` não absorve o `FvToggle` (o `toggleable` já é nó agrupador): "Botão flutuante" é lido na linha e no interruptor, com a dica solta. "Novo termo" (Dicionário) e "Google Web Client ID" (`FvDarkField` com `label = placeholder`) são lidos duas vezes com o campo vazio | pendente; metade resolvida pela P109 (0.4.3): "Novo termo" e "Google Web Client ID" já não duplicam com o campo vazio. Resta `SettingsRow` + `FvToggle` (`SettingsRow.kt:71`, `SettingsRoute.kt:366,377`) e o placeholder "Escreva ou dite…" exposto como texto solto nas Notas (`NotesRoute.kt:483-487`) | /corrigir |
| P97 | Baixa | O CHANGELOG 0.4.2 afirma como fato que "a barra acompanha o teclado; o fallback não usa texto antigo" (P81), mas isso não foi medido no aparelho, a limpeza só roda na API 33+ e, abaixo disso, o fallback (única rota) segue exposto; o comentário de `accessibility_flowvoice.xml:2-4` induz a erro (o cache é mantido, mas não invalidado) | pendente | /corrigir (docs) |
| P98 | Baixa | P66: o texto do onboarding não diz "tente ligar primeiro" (o item "Permitir configurações restritas" só aparece depois de uma tentativa bloqueada; o README diz); `InstallSourceInfo.getPackageSource()` (API 33) seria um sinal mais preciso que o instalador | pendente | /aprimorar |
| — | — | **/verificar da 0.4.3 (2026-09-14, `8cc4bb3`): APROVADO COM RESSALVAS.** Clone novo e build sem cache (102 tarefas executadas; 329 testes, 0 falhas; lint 15 avisos; APK `versionCode` 8) e CI `34828729881` verde. P99, P100, P101, P102, P103, P107, P108 e P109 fecham o cenário original. Achados novos: P110–P122. Aparelho não exercitado (Discador em primeiro plano) | — | — |
| P110 | Alta | Coordenador de nota fica armado depois de uma segunda tentativa sem chave, e o texto de outro app vai para a nota. Confirmado por teste descartável (`verificarReproSecondKeylessNoteAttempt`, clone de `8cc4bb3`): depois da 2ª tentativa `activeNoteId=B` e sem mensagem; com a chave configurada, uma sessão de campo ativo inseriu "texto do whatsapp w1" **e** gravou o mesmo texto na nota B. Causa: `start()` grava `Starting` e `Failed(chave)` sem suspender (`DictationPipeline.kt:136-146`); o `StateFlow` não emite o `Failed` igual ao anterior, e o `NoteDictationCoordinator` (baseline = esse `Failed`, `:44`) nunca vê a sessão terminar. Enquanto isso as Notas mostram "Ouvindo…" na nota B durante qualquer sessão de campo ativo, e o Parar chama `finalize()`, que insere sem toque em Inserir (`:190-208`, `:331`). Mesma raiz: no Início a 2ª tentativa mostra "O microfone não respondeu a tempo." (`HomeRoute.kt:202-210`, `it !== previous`) e o `OverlayOwnership` fica com baseline sem liberação. A mensagem do commit `7e7f13c` ("um Failed igual ainda chega aos observadores") está errada. Correção proposta: o coordenador só acompanha sessão com destino `Note`, identificada por sessão e não por igualdade de status; `finalize()` de campo ativo vai para a revisão | concluída (0.4.4): sessões com identidade no pipeline, coordenador de nota, Início e barra; Parar em campo ativo vai para a revisão. Validada no S26 em 2026-09-14 (ver P94) | /corrigir |
| P111 | Alta | Chave rejeitada no meio da gravação descarta todo o texto já transcrito: `failOnInvalidKey` (`DictationPipeline.kt:172-179`) faz `sessionToken++`, `transcription.cancel()` e `Failed`; numa nota nada é anexado (o coordenador só anexa em `Completed`). A mesma rejeição em `Transcribing` mantém o texto como parcial, então o comportamento é inconsistente. O CHANGELOG 0.4.3 não menciona a perda. Correção proposta: parar a captura e entregar o texto parcial com aviso (`Ready` no campo ativo, `Completed` na nota) | concluída (0.4.4): a captura para e o texto é entregue com aviso (`Ready` no campo ativo, `Completed` na nota); sem texto, "Nenhum trecho transcrito (chave…)"; revisão por IA pulada com chave recusada | /corrigir |
| P112 | Média | HTTP 403 tratado como chave inválida e, desde a P107, fatal: pela documentação da OpenRouter (`/docs/api-reference/errors`), 403 é "Forbidden (insufficient permissions, guardrail block, or moderation flag)". `TranscriptionErrorClassifier.kt:13` mapeia `401, 403 -> InvalidKey`, então um bloqueio de guardrail ou moderação num trecho encerra a sessão com "chave OpenRouter ausente ou inválida" e perde o texto (P111). Correção proposta: só 401 é fatal; 403 falha o trecho com mensagem própria | concluída (0.4.4): só 401 é chave inválida; 403 vira `Forbidden`, falha o trecho com "recusado pela OpenRouter" e a sessão continua | /corrigir |
| P113 | Média | Depois de uma recusa de destino, o próximo toque em Inserir recaptura o app em foco e insere ali (`DictationPipeline.kt:264`, desenho da P101). A mensagem "o foco mudou de app; texto mantido na barra" não avisa isso, e a barra não trava toques seguidos: um toque duplo em Inserir leva o texto ao app atual sem confirmação. README e CHANGELOG não descrevem. Proposta: mensagem explícita ("toque de novo para inserir aqui") e ignorar toques durante a inserção | concluída (0.4.4): aviso "o foco mudou de app; toque em Inserir de novo para inserir no app atual" (texto escolhido pelo usuário), toque até 1 s depois da recusa ignorado e README descreve. Falta ver no S26 (P94) | /corrigir (texto a combinar com o usuário) |
| P114 | Média (suspeito) | Sucesso falso sem campo focado: no mesmo app do início, numa tela sem campo (ex.: lista de conversas do WhatsApp), a conferência passa (janela ativa = app do início) e `insertDirect` usa `currentInputConnection` e `EditorInfo` do último editor, a mesma premissa da P108 (`FlowVoiceAccessibilityService.kt:99-125`). `commitText` não devolve resultado, o fallback não roda, a barra mostra "Inserido no campo ativo" e fecha em 4 s com o texto perdido. Validar no S26 | pendente; avaliada na 0.4.5 sem aparelho e não corrigida: comparar o pacote do editor com a janela ativa não pega o caso (mesmo app), e exigir um nó com foco de entrada editável antes do `commitText` pode recusar inserções que hoje funcionam (views sem nó editável, WebViews). Precisa do /debugar no S26 antes | /debugar |
| P115 | Média (suspeito) | Com TalkBack (exploração por toque), a janela ativa passa a ser a janela tocada, o overlay; `focusedPackage()` devolveria o pacote do FlowVoice e `InsertionGuard.refusal` recusaria sempre ("FlowVoice em primeiro plano"); a nova tentativa recaptura `null` e recusa de novo. Antes da P108 o `EditorInfo` vinha primeiro. Validar no S26 com TalkBack (P94) | pendente | /debugar |
| P116 | Baixa | CHANGELOG 0.4.3 impreciso: a P107 omite a perda do texto com chave rejeitada no meio (P111); a P109 afirma como fato que os campos "anunciavam o rótulo no lugar do texto", mas o efeito no TalkBack não foi medido (mesmo padrão da P97), e "os campos de Ajustes ganharam nome" só vale com o campo vazio. As mensagens de `526ce09` e `7e7f13c` dizem "nenhum teste antigo alterado", mas `reachingRequestBudgetStopsCaptureAndCompletesWithWarning` foi substituído no intervalo (a cobertura foi mantida por `reachingRequestBudgetInActiveFieldSessionStopsCaptureAtReady`) | pendente | /corrigir (docs) |
| P117 | Baixa | README ("Ditando") e `docs/tasks/F12.md:29-31,36-40` desatualizados pela 0.4.3: não mencionam o Parar na barra que assume ditado de nota, a falha imediata sem chave nem o novo toque que insere no app atual; "toque durante a gravação finaliza e insere" e o Toast de chave "sem nenhum trecho transcrito" já não valem | pendente | /corrigir (docs) |
| P118 | Baixa | Campos "Teto (USD)" e "Texto de referência (pt-BR)" do Diagnóstico sem nome acessível: o rótulo é um `Text` irmão e o `FvDarkField` não recebe `label` nem `placeholder` (`DiagnosticsRoute.kt:332-349`), então `fieldDescription` não gera nome (P109 não cobre) | pendente | /corrigir |
| P119 | Baixa | Efeito colateral da P102: com a nota B em ditado, tocar no cartão da nota A no Início abre a B. `NoteSelection.initial` prefere a nota em ditado mesmo com pedido explícito (`NotesScreenState.kt:141-142`), e `initialSelectionPrefersTheDictatingNoteOverTheRequestedOne` fixa isso; a preferência só é necessária na recriação da tela | pendente | /corrigir |
| P120 | Baixa (suspeito) | `FocusedPackage.resolve` usa o `EditorInfo` quando a janela ativa é desconhecida e `inputStarted` é true. Pelo diagnóstico da P108, o input não é finalizado ao sair do campo, então isso reabre o defeito nas transições em que `rootInActiveWindow` é nulo; a proposta era "input iniciado **e** pacote igual", e `startedEditorIsUsedOnlyWhenActiveWindowIsUnknown` fixa a versão fraca | pendente; não corrigida na 0.4.5: com a janela ativa desconhecida, devolver `null` desliga a conferência (`InsertionGuard.refusal` só recusa com os dois pacotes conhecidos) e o `commitText` iria para o editor antigo, que pode ser de outro app; decidir junto com a P114 o que fazer com foco desconhecido | /corrigir |
| P121 | Baixa (suspeito) | Ditado de nota com o app em segundo plano: as Notas chamam `pipeline.requestStart(DictationTarget.Note)` sem subir o serviço em primeiro plano de microfone (`NotesRoute.kt:96-104`); ao sair do app, a captura pode ficar muda ou seguir sem controle até o teto (~2 min, 30 requisições), e janelas de silêncio podem virar texto inventado anexado à nota. Validar no S26 | parcial (0.4.5, via P124): janela de silêncio digital não vai mais à OpenRouter nem vira texto, e a captura muda ainda para no teto. Falta decidir se o ditado de nota sobe o serviço de microfone ou para ao sair do app (com a bolha ligada o serviço já existe e a captura segue com áudio real) e validar no S26 que a captura em segundo plano chega zerada (P94). **S26 (2026-09-14, 0.4.5):** com o serviço de acessibilidade ligado e a bolha desligada, a captura em segundo plano **não** foi silenciada (`dumpsys audio`: `silenced:false` com o launcher em foco) e seguiu enviando áudio real até o teto (~2 min, 30 requisições), sem notificação de serviço em primeiro plano; o PCM zerado da P124 não ocorreu nesse aparelho. A decisão (subir o serviço ou parar ao sair do app) continua com o usuário | /debugar |
| P122 | Baixa | Lacunas de teste da 0.4.3: coordenador com baseline igual ao novo status (P110); o ramo `fatalError` durante `Starting` (`DictationPipeline.kt:158`) nunca executa com o `ScriptedAudioCaptureEngine`; recusa com o próprio FlowVoice em foco dentro do `AccessibilityTextInserter`; `budgetIsCountedAtSubmission…` aceita `1..3` quando só 3 é possível; ligação real Parar → `requestFinalize` e `onDestroy` lendo `target` sem teste (ver P68/P105) | parcial (0.4.5): baseline igual no coordenador; ramos de chave recusada e de teto durante `Starting` (removendo o ramo, só os dois testes novos falham); teto com valor exato. Faltam a recusa com o FlowVoice em foco no `AccessibilityTextInserter`, Parar → `requestFinalize` e `onDestroy`, que dependem de teste no `androidApp` (P68) | /aprimorar |
| P123 | Média | No S26 (2026-09-14), a bolha flutuante na posição padrão fica sobre o botão de microfone "Ditar nesta nota" das Notas ([1182,2606][1350,2774]) e engole o toque: nada acontece, sem log; só o rótulo ao lado inicia o ditado. Mesma causa da P27 (bolha fixa sobre controles) | concluída (0.5.0, via P138): a posição padrão passou a direita a 30 % da altura (1132,955 no S26, acima do teclado), e a bolha pode ser arrastada para longe de qualquer controle | /corrigir |
| — | — | **Revisão do pipeline de voz (2026-09-14, sobre `f4b773d`):** achados P124–P127, sem aparelho; P126 e P127 confirmados por teste descartável, não commitado. **0.4.5:** concluídos P124 e P125; parciais P121 (via P124) e P122; P114 e P120 avaliados e não corrigidos sem o S26; P126 espera o texto do motivo; P127 espera medição | — | — |
| P124 | Média | Janelas de silêncio digital vão à OpenRouter: `IncrementalTranscriptionController.process` (`:94-130`) envia toda janela, inclusive PCM zerado, que é o que chega quando o sistema silencia a captura (app em segundo plano sem serviço de microfone em primeiro plano, cenário da P121). Cada janela custa uma requisição e o modelo pode devolver texto inventado, anexado à nota ou levado à barra. Correção proposta: não enviar janela de silêncio digital, marcar o trecho como vazio e manter a vaga no teto, para a captura muda ainda terminar | concluída (0.4.5): `SilentWindow` — janela com pico até 16 (~-66 dBFS) vira trecho vazio sem requisição (`transcription_silent_window`) e segue contando no teto; fala baixa e ruído de sala continuam indo à API. Limiar não medido; validar no S26 (P94). No S26 (2026-09-14) o caso não ocorreu: a captura em segundo plano chegou com `silenced:false` (P121), então nenhuma `transcription_silent_window` foi registrada e a correção segue sem exercício no aparelho | /corrigir |
| P125 | Baixa | HTTP 408 da OpenRouter ("Your request timed out", `/docs/api-reference/errors`) cai no `else` de `TranscriptionErrorClassifier.fromHttpStatus` (`:12-27`) e vira `InvalidResponse`: o `RetryingTranscriptionClient` não repete o trecho e o aviso diz "resposta inválida" em vez de "timeout". Correção proposta: 408 → `Timeout` | concluída (0.4.5): 408 → `Timeout`, repetido pelo `RetryingTranscriptionClient`; se falhar de vez, o motivo é "timeout" | /corrigir |
| P126 | Média | HTTP 402 ("Your account or API key has insufficient credits") também vira `InvalidResponse`. Confirmado por teste descartável: cada janela faz a requisição e falha sem repetir, a sessão segue gravando até o usuário parar ou até o teto (30 requisições, ~2 min) e termina em "Nenhum trecho transcrito (resposta inválida)", sem dizer que faltam créditos. Correção proposta: parar a captura como na chave recusada (P111), entregando o texto já transcrito, com motivo próprio | pendente: texto do motivo a combinar com o usuário | /corrigir (texto a combinar) |
| P127 | Baixa | Palavra repetida na fronteira entre janelas some: `LivePreviewAssembler.mergeAdjacent` (`:31-47`) remove o maior sufixo do trecho anterior igual ao prefixo do seguinte, como se as janelas se sobrepusessem (`docs/tasks/F06.md:6,27`), mas o `DictationWindowAggregator` corta áudio contíguo, sem sobreposição (`docs/PLAN.md:51`). Confirmado por teste descartável: "é muito" + "muito importante" → "é muito importante"; "tomar 1" + "1 comprimido" → "tomar 1 comprimido". A deduplicação pode estar evitando palavra dobrada quando o corte cai no meio dela; decidir com fala real (F05) antes de mudar | pendente: medir antes de mudar | /debugar |
| P128 | Baixa | Nas Notas, um aviso local antigo esconde o resultado do ditado: `NotesScreenState.message` (`:39-42`) prefere `localMessage` à mensagem do coordenador. No S26 (2026-09-14, 0.4.5), um toque em "Nova nota" com ditado em andamento deixou "Já há um ditado em andamento." na tela e, quando a sessão terminou no teto, "Nenhum trecho transcrito (teto de requisições da sessão)" não apareceu; numa sessão sem aviso local a mesma mensagem apareceu. Correção proposta: o fim da sessão limpa o aviso local ou tem precedência sobre ele | pendente | /corrigir |
| P129 | Baixa | "Nova nota" com ditado em andamento cria uma nota vazia: `onNewNote = { requestDictation(state.createNote().id) }` (`NotesRoute.kt:147`) cria a nota antes da checagem de ocupado em `startDictation` (`:96-100`). No S26 (2026-09-14, 0.4.5) o segundo toque criou uma nota "Sem título" vazia e mostrou "Já há um ditado em andamento.". Correção proposta: checar `isBusy` antes de criar a nota | pendente | /corrigir |
| — | — | **Teste com fala no S26 (2026-09-14, 0.4.5, 19:44–19:46) e revisão de código em paralelo:** achados P130–P135. Três ditados no Samsung Notes: o 1º pelo microfone do Início, o 2º e o 3º pela bolha. Nenhuma janela falhou (`failedWindows=0`, sem `transcription_window_failed`), e o 2º e o 3º entraram na ordem (P20) | — | — |
| P130 | Alta | O microfone do Início não volta ao app em que o usuário estava, ao contrário do que diz o README.md:77. `HomeRoute.kt:206` só chama `moveTaskToBack(true)`, que mostra a tarefa de baixo, e o app de origem nunca é gravado. Confirmado no S26: Samsung Notes (`ComposerActivity`, 19:44:33) → Recentes → FlowVoice (19:44:48) → microfone. `dictation_started` saiu às 19:44:56.573 e, 5 ms depois, `wm_set_resumed_activity` apontou `LauncherActivity`: no One UI até a troca pelos Recentes deixa o launcher embaixo. Consequência: o toque em Inserir com o launcher em foco foi recusado (`dictation_insert_refused`, 19:45:12), e o usuário teve de abrir as Notas na mão e tocar de novo (19:45:24). Correção proposta: o serviço de acessibilidade guarda o último pacote com janela ativa fora do FlowVoice e do launcher. Quando o status chega a `Recording`, o Início abre esse pacote com `getLaunchIntentForPackage`, que retoma a tarefa existente como o toque no ícone fez às 19:45:22, e só usa `moveTaskToBack` quando não há pacote conhecido | concluída (0.4.6, `ea8e2a6`): o serviço assina só `typeWindowStateChanged` e anota o pacote da janela ativa (`PreviousApp`); o Início reabre esse app pela entrada do launcher, com `<queries>` no manifesto. **Validada no S26 (2026-09-14, 0.4.6, sem fala):** Início → Notas → Início → ícone do FlowVoice → microfone: `dictation_started` às 21:17:15.611 e `wm_set_resumed_activity` do `ComposerActivity` das Notas às 21:17:15.623, com a barra "Ouvindo" sobre o teclado; ditado cancelado em seguida. Falta ver: entrada pelos Recentes e a bolha de outro app (Wispr Flow) não virar destino. **Revisão de código (`f60b5b0`):** não há mais `rootInActiveWindow` a cada evento. O pacote do evento só conta se a janela ativa na lista do sistema for a dele e do tipo aplicação: medido no S26, a activity que abre chega assim, e bolhas, teclado, cortina e painéis do launcher não. Apps abertos pelo FlowVoice (Ajustes, compartilhamento) não viram destino até passar pelo launcher. Revalidada no S26 às 21:43 (Notas voltaram à frente). **Entrada pelos Recentes validada (2026-09-15 12:35, 0.4.7):** Notas → Recentes → FlowVoice (12:34:54) → microfone: `dictation_started` às 12:35:14.615 e `ComposerActivity` das Notas retomada às 12:35:14.635, com o texto inserido. **Bolha de outro app (2026-09-15 12:40, 0.4.7):** o Wispr Flow não roda no S26 e não tem permissão de sobreposição (só Galaxy Store, app Google e FlowVoice têm), então a bolha dele não foi reproduzida. Pelo `dumpsys accessibility`, a bolha do FlowVoice, que é uma janela de sobreposição de app do mesmo tipo da do Wispr, é `TYPE_SYSTEM`, inativa e sem foco, tanto parada (1132,2465–1398,2731) quanto tocada e aberta como barra de ditado (0,1312–1440,1652). A janela das Notas continuou a única `TYPE_APPLICATION` ativa. Na medição de 2026-09-14 21:39, a sobreposição do app Google (terceiro) nem entrou na lista de janelas. Como o `PreviousAppTracker` exige janela ativa `TYPE_APPLICATION`, bolha de app nunca vira destino. Considerada validada por essa evidência; ver com o Wispr ativo se ele for instalado com a bolha. Limites conhecidos: uma activity de outro pacote dentro da tarefa (Custom Tab, câmera) faz o destino ser o app dela, e `homePackages` só é lido ao conectar o serviço | /corrigir |
| P131 | Média (suspeito) | Palavra cortada entre janelas pode virar outra: no 3º ditado, "Terceiro ditado pela bolha" saiu "Terceiro colocado pela bolha" (janelas de 4096 ms + 2304 ms, sem falha). `DictationWindowAggregator.onFrame` (`:25-26`) corta o áudio contínuo a cada 4 s ou mais, sem sobreposição e sem esperar pausa na fala. `OpenRouterTranscriptionClient` (`:46-53`) envia cada janela só com `language`, sem o texto anterior como contexto. Se "di\|tado" caiu no corte, a 2ª janela começa em "…tado". Hoje não dá para confirmar: o texto de cada janela não é registrado (P135), e a revisão por IA também pode ter trocado a palavra (P132). Medir com o texto por janela antes de mudar. Candidatos: cortar na pausa (menor energia perto dos 4 s) ou sobrepor as janelas e deduplicar (ligado à P127) | concluída (0.4.6, `719713b`): corte no meio do trecho de 20 ms de menor energia entre 3,4 s e 4,6 s (`pauseSearchMs` = 600 ms), resto do áudio vai para a janela seguinte. No S26 (0.4.6, sem fala, ruído de sala) as janelas saíram com 3770, 4370, 3810 e 4350 ms, e nenhuma virou texto. A causa não foi medida: falta repetir "Terceiro ditado pela bolha" e comparar `transcription_window_text` com a fala. **Revisão de código (`57679e2`):** o corte usa a menor energia média em 120 ms (a pausa de uma oclusiva não conta), a busca vai de alvo − 900 a alvo + 300 ms e a linha do tempo vem dos bytes. Rever a P127 com fala: com o corte na pausa, palavra repetida na fronteira passa a ser mais provavelmente repetição real. **Medido com fala no S26 (2026-09-15, 08:05, texto por janela ligado):** nos três ditados a frase inteira coube na primeira janela (4140, 4240 e 3900 ms), e a segunda janela veio sem texto. O corte não partiu nenhuma palavra: os erros vieram da transcrição dentro de uma janela só (ver P136) | /corrigir |
| P132 | Média (suspeito) | A revisão por IA reescreve o conteúdo. O 1º ditado, falado como "Primeiro ditado pelo início", foi inserido como `Primeiro ditado: "Pelo início."`: dois-pontos e aspas inventados, embora o prompt peça "sem aspas". `OpenRouterProofreadingClient` (`:40-46`, `:58`, `:69-70`) envia o texto cru como mensagem `user` para `openai/gpt-4o-mini`, com prompt de uma frase, e aceita qualquer resposta não vazia. Frase com cara de título, pergunta ou instrução pode voltar reescrita ou respondida. Não se sabe se a troca veio da transcrição ou da revisão: o log só traz `proofreading_applied chars=`. Correção proposta: delimitar o texto no prompt, proibir acrescentar citação ou responder, e descartar a revisão quando ela divergir demais do original (palavras novas ou razão de tamanho) | concluída (0.4.6, `8772099`): texto entre `<ditado>`, prompt que proíbe responder, trocar palavras e acrescentar citação, e `ProofreadingGuard` (aspas novas ou distância de edição acima de max(2, tamanho/20) no texto sem acentos e pontuação → `proofreading_rejected`). **Revisão de código (`d705432`):** o orçamento por caracteres aceitava "500 mg" → "50 mg", "não" removido e lateralidade trocada em texto de uma ou duas frases. O guard agora compara por palavras (mesma contagem; números e palavras curtas idênticos; palavras de 5 letras ou mais com até 2 edições) e rejeita `<ditado>` sobrando e aspas, dois-pontos ou quebra de linha novos. **Medido com fala no S26 (2026-09-15):** a revisão só mexeu em pontuação ("Primeiro digitando pelo início." → "Primeiro, digitando pelo início."; ponto final no segundo; terceiro igual), sem aspas nem troca de palavra. Os erros de palavra já vinham da transcrição (P136) | /corrigir |
| P133 | Média | O Início avisa "O microfone não respondeu a tempo." com o microfone aberto. `HomeRoute.kt:201-209` desiste ao fim de `START_TIMEOUT_MS` (`withTimeoutOrNull` devolve `null`) sem cancelar a sessão pedida ao `FlowVoiceOverlayService`. Se `Recording` chegar depois (serviço e overlay frios, `AudioRecord` com prazo próprio de 3 s), a gravação continua, sem retorno ao app e com mensagem de falha na tela. Não observado no S26: com o serviço já vivo, Starting → Recording levou 39 ms. Risco de privacidade em uso clínico. Achado da revisão de código | concluída (0.4.6, `f091bb8`): no timeout, a sessão seguinte à anterior é cancelada se estiver ocupada ou começar em até 10 s (`cancelAfterStartTimeout`); o aviso diz "ditado cancelado". **Revisão de código (`fa83d12`):** a espera rodava no escopo da tela, e girar o aparelho ou sair do Início deixava o microfone aberto; um segundo toque tinha a própria sessão cancelada. Agora a espera e o cancelamento ficam em `DictationPipeline.abandonStart`, no escopo do pipeline, e cada toque chama `clearAbandonedStart` | /corrigir |
| P134 | Média (suspeito) | A chave OpenRouter é lida do cofre a cada janela: `IncrementalTranscriptionController.kt:111-115` chama `apiKeyProvider()` por janela. `CipherSecretStore.readOpenRouterKey` (`:23-45`) devolve `null` em qualquer falha de leitura ou decifragem, e apaga o cofre quando `isCorruption`. Isso vira `InvalidKey` fatal e encerra a sessão no meio do ditado com "chave ausente ou inválida". Correção proposta: ler a chave uma vez em `start()`. Achado da revisão de código, não observado | concluída (0.4.6, `620aec5`): `start()` lê a chave uma vez e a entrega a `IncrementalTranscriptionController.reset(apiKey)`; a revisão usa a mesma. **Revisão de código:** a sugestão de zerar a chave ao fim da sessão não foi aplicada: uma `String` em Kotlin não pode ser zerada, e manter a chave durante todo o ditado, mesmo se ela for apagada nos Ajustes no meio, é o objetivo da P134 | /corrigir |
| P135 | Média | Falta observabilidade para diagnosticar a transcrição no aparelho. O texto de cada janela e o texto antes e depois da revisão nunca são registrados (só contagens). `OpenRouterTranscriptionClient` é criado sem `eventLog` (`AppModule.kt:22-25`, padrão `TranscriptionEventLog.NoOp`), então status HTTP e repetições não saem no logcat. "Exportar relatório" também não inclui eventos (`DiagnosticReport.kt`). Proposta: só em build de depuração, registrar o texto por janela e o texto antes e depois da revisão (nunca em release, nunca a chave), e ligar o `eventLog` com status HTTP e tentativas | concluída (0.4.6, `9eb9560`): `transcription_error status=`, `transcription_retry` e `proofreading_applied inputChars=`; só em build debuggable, `transcription_window_text` e `proofreading_text`. No S26 (0.4.6) `transcription_request`, `transcription_success` e `transcription_window_text` já aparecem no logcat. "Exportar relatório" continua sem eventos. **Revisão de código (`f731fe6`):** a trava só por `FLAG_DEBUGGABLE` deixava o texto sempre no log, porque o S26 só instala o APK de depuração, e ele também aparecia na tela de Diagnóstico. Agora o texto exige também o marcador `files/transcript-text-logging` (`adb shell run-as dev.rafaelbrauner.flowvoice touch files/transcript-text-logging`) criado há menos de 1 h, vai só para um log de texto separado (nunca `eventLines`/Diagnóstico), sai em `proofreading_input`/`proofreading_output` e linhas longas são partidas. Usada com fala no S26 em 2026-09-15 (marcador às 08:04:17) | /corrigir |
| P136 | Média | `openai/gpt-4o-mini-transcribe` erra "ditado" em frase curta: no S26 (2026-09-15, 0.4.6, três ditados pela bolha nas Notas), `transcription_window_text` deu "Primeiro digitando pelo início." e "Terceirizado pela bolha." para "Primeiro ditado pelo início" e "Terceiro ditado pela bolha"; "Segundo ditado pela bolha" saiu certo. Frase inteira numa janela só, sem falha e sem mudança de palavra na revisão: o erro é do modelo de transcrição, e é a causa do "Terceiro colocado" de 2026-09-14 (P131/P132 descartadas como causa). A API de transcrição da OpenRouter não tem `prompt` para dar contexto ou vocabulário: aceita só `language`, `temperature`, `response_format`, `timestamp_granularities` e `provider` (`/docs/api/api-reference/stt/create-transcription`). Proposta: rodar o Benchmark F05 no S26 (Diagnóstico → "Rodar rodada 1", clipe = última sessão, 8 modelos da `TranscriptionShortlist`) com frases de ditado reais e trocar o modelo padrão se outro errar menos; avaliar `temperature` 0 | concluída (0.4.7): Benchmark F05 no S26 em 2026-09-15 (clipe "Terceiro ditado pela bolha. Paciente refere dor no joelho direito, sem febre e sem alergia a dipirona.", 17 palavras, 8/8 ok, US$ 0,0047) deu `openai/gpt-transcribe` WER 0 (1420 ms, US$ 0,0011), `deepgram/nova-3` e `gpt-4o-mini-transcribe` WER 0,06 e os demais 0,12–0,18. Com a rodada de 13/set (os dois OpenAI com WER 0), o padrão passou a `openai/gpt-transcribe`. Uma primeira execução saiu com a referência padrão ("O médico pediu…") e foi descartada. No mesmo ditado, com o modelo antigo, o corte em 3380 ms fez a janela seguinte começar em "Biela Bolha" ("pela bolha"): janela sem contexto também erra na fronteira. **Validada no S26 (2026-09-15, 0.4.7, `openai/gpt-transcribe`):** às 12:35, com texto por janela ligado, os três ditados saíram corretos na primeira janela: "Primeiro ditado pelo início.", "Segundo ditado pela bolha." e "Terceiro ditado pela bolha." (3940, 4060 e 3740 ms; segunda janela vazia). A revisão não mudou nada, nenhuma janela falhou e a latência até pronto ficou em 2,5–3,2 s. Uma rodada anterior, às 12:34 e sem o marcador, teve os mesmos tamanhos (28, 26 e 27 caracteres). `temperature` não foi testada | /verificar |
| P137 | Baixa | A transcrição não envia `temperature`, então o resultado depende do padrão de cada provedor. A OpenRouter só documenta o intervalo ("Sampling temperature between 0 and 1. Lower values produce more deterministic results"), sem padrão, e o SDK da OpenAI também não diz o padrão ("If set to 0, the model will use log probability to automatically increase the temperature until certain thresholds are hit"). Com `gpt-transcribe` os três ditados curtos já saíram certos, então não há erro para medir: a correção é tirar a dependência do padrão | concluída (0.4.8): `OpenRouterConfig.temperature` = 0.0 (validado em [0, 1]) vai em todo `transcription_request`. **Validada no S26 (2026-09-15 12:43, 0.4.8):** pelo microfone do Início, `transcription_request` com `openai/gpt-transcribe` voltou `transcription_success` (3560 ms de áudio), sem `transcription_error`. As Notas voltaram à frente (P130) e o ditado de teste foi cancelado | /corrigir |
| — | — | **Bolha arrastável e inserção direta (2026-09-15, 0.5.0):** P138 e P139. O desenho e as decisões estão em [`docs/tasks/P138-P139.md`](docs/tasks/P138-P139.md). Sem fala no S26: arraste, posição lembrada, rotação, toque curto, prévia e cancelamento. A digitação com fala fica para o usuário | — | — |
| P138 | Média | A bolha flutuante fica presa no canto inferior direito, sobre o teclado (1132,2465–1398,2731 no S26 de 1440×3120), e não pode ser movida. Cobre a tecla de ação e o microfone "Ditar nesta nota" (P27, P123) | concluída (0.5.0): a bolha é arrastável e encaixa na borda mais próxima ao soltar, dentro da área segura (barras, recorte e 12 dp). A posição fica salva como lado + fração da altura, em `flowvoice_overlay`, fora da sincronização. O toque curto e o arraste se separam pelo *slop*, e o arraste nunca inicia ditado. No TalkBack a bolha tem nome, papel de botão e as ações "Mover para cima", "Mover para baixo" e "Mover para o outro lado". **Validada no S26 (2026-09-15, sem fala):** a posição padrão ficou em (1132,955). O `input swipe` levou a bolha à esquerda (`overlay_bubble_moved source=drag side=left fraction=0.73`) e de volta à direita (0.23, janela em 1132,771), sem `dictation_started`. A preferência (`bubble_fraction=0.22856034`) e a janela (1132,771) voltaram iguais depois de reinstalar o app com o processo morto e depois de girar para paisagem (2812,360, borda direita) e voltar a retrato. As ações do TalkBack não foram exercitadas no aparelho (só a lógica tem teste) | /construir |
| P139 | Alta | Ditar em outro app pede cinco passos: gravar, ver a barra, tocar em Inserir, ver o texto final e tocar de novo. O usuário quer tocar na bolha e ver o texto sendo digitado no campo aberto, com uma prévia do que está sendo transcrito e do que já foi escrito | concluída (0.5.0): no modo direto (padrão), cada janela resolvida é digitada em ordem (`DirectInsertionPlanner`), com espaço e sem repetir palavra na emenda. Tocar na bolha de novo encerra. A digitação sem toque exige destino conhecido, o mesmo app e, depois do primeiro trecho, o mesmo input (`DirectInsertionGuard` + geração de input do serviço de acessibilidade). Qualquer recusa pausa até o fim, e o resto vai para o pendente, que só "Inserir aqui" escreve. Não há revisão por IA no modo direto; o ajuste "Revisar antes de inserir" mantém o fluxo antigo. O microfone do Início fixa o destino no app que reabre. A prévia fica colada à bolha, com NO CAMPO, transcrevendo…, PENDENTE e avisos. **Parcial no S26 (2026-09-15, sem fala):** o toque curto na bolha iniciou o ditado (`dictation_started` às 14:33:10.829), e a prévia abriu ao lado da bolha (janela em 42,771 com 1356 px; "OUVINDO · TOQUE NA BOLHA PARA ENCERRAR" e Cancelar). O ditado foi cancelado em 2,3 s pelo receiver de depuração (`dictation_cancelled`), sem `transcription_request`. Numa nota do próprio FlowVoice com o teclado aberto (14:37:45–47, inserção bloqueada por ser o próprio app), o cartão abriu sobre a 3ª e a 4ª linha do corpo, **cobrindo a linha do cursor**: com a bolha perto do texto, a prévia colada à bolha cobre o campo. O toque na bolha e a prévia não mudaram a geração de input (nenhum `input_generation` no intervalo), e a nota e o cursor ficaram intactos depois do cancelamento. **Decisão do usuário (2026-09-15): prévia longe do cursor.** O cartão ganhou janela própria. `PreviewPlacement` escolhe o lado acima ou abaixo da linha do cursor, que o serviço de acessibilidade informa só como coordenada (`EXTRA_DATA_TEXT_CHARACTER_LOCATION_KEY`). Na falta da linha, vale o campo inteiro, se for pequeno; na falta dos dois, a regra antiga pela metade da bolha. O cartão não entra no teclado e encolhe ou some quando falta espaço. **No S26 depois da mudança (15:05–15:11, nota do FlowVoice e Ajustes, microfone cancelado em até 3 s):**
- Bolha baixa (67 %, 1132,1915) com o cursor no alto da nota: `avoiding=caret opens=below edge=1313 caret=873..948 keyboardTop=1705`, e o cartão ficou entre a linha "dois." e o teclado.
- Bolha a 30 % (1132,957): `opens=below edge=1065 maxHeight=612`, abaixo da linha.
- Duas sessões seguidas: cartão completo nas duas, depois da correção do view reaproveitado, que aparecia como uma faixa vazia.
- Campo vazio "Google Web Client ID" nos Ajustes: `avoiding=field opens=below edge=957`, abaixo do campo (688–838).
- Cursor no fim do "Texto de referência" do Diagnóstico, na base da tela, com o teclado fechado: `caret=2326..2389 opens=above edge=2176`, acima da linha. Com o teclado aberto, a própria tela subiu o campo, e o cartão foi para baixo da linha, acima do teclado (bolha a 30 %: `edge=962`; bolha a 67 %: `edge=1313`). 17:55.
- Paisagem com o teclado quase na tela toda (`keyboardTop=496`), com a bolha baixa e com ela alta: `overlay_preview_hidden reason=no_room`, e nenhum cartão apareceu.
- Limite visto em paisagem (15:13): sem campo em foco, o cartão do fallback abriu sobre a linha que eu ia tocar para focar e recebeu o toque.
- Bolha devolvida ao padrão (preferência apagada e app reinstalado): janela em (1132,955).

Ao longo do caminho, dois defeitos foram achados e corrigidos: a leitura fora da thread principal devolvia campo e linha nulos, e o `ComposeView` do cartão recolocado na janela ficava vazio. **Falta com fala:** a digitação no Samsung Notes e no WhatsApp, a pausa ao trocar de app ou de conversa, o "Inserir aqui" e se algum app reinicia o input a cada `commitText` (pausaria a cada trecho: `dictation_direct_paused route=trava:campo`) | /verificar (com fala) |
| — | — | **Latência da transcrição ao vivo (2026-09-15, 0.5.0):** P140. Medida no logcat do usuário (S26, `openai/gpt-transcribe`); a correção foi feita sem aparelho e espera a validação com fala | — | — |
| P140 | Alta | Texto ao vivo com latência alta no modo direto (P139). No S26 (2026-09-15, 0.5.0, `openai/gpt-transcribe`), num ditado de 23 s no Samsung Notes: `dictation_started` às 18:15:56.752, primeiro `transcription_request` às 18:16:01.176 (janela de 4200 ms), `transcription_success` às 18:16:02.838 e texto inserido **6,1 s depois do início**. Os pedidos seguintes saíram a cada 3,4–4,3 s (janelas de 3380, 4060, 3360, 4020 e 3764 ms), e a OpenRouter respondeu em 1,1–1,7 s. Cada palavra esperava em média ~4 s para aparecer, e o que dominava era o tamanho fixo da janela (`DictationWindowAggregator`, alvo de 4 s), não a rede. A captura em frames de 256 ms (`readBufferBytes=8192`) limitava a resolução de qualquer detecção de pausa, e com janelas menores o teto de 30 pedidos acabaria em ~1 min | concluída (0.5.0, sem aparelho): `SpeechEndpointing` corta a janela na pausa natural (`47cda41`): ≥ 1,2 s acumulados, ≥ 400 ms de fala (só trechos de 80 ms ou mais contam, e estalo de tecla não libera o corte) e 300 ms de pausa, com o corte no meio da pausa. Sem pausa, vale o teto de 4,3 s no trecho mais silencioso (`cut=ceiling`), ou logo antes da fala que começou depois de um trecho sem fala (`cut=leading`). Os limiares seguem o piso de ruído: percentil 10 dos níveis (média do valor absoluto em blocos de 20 ms) dos últimos 10 s. Pausa é até 2× o piso (mínimo 100), fala é acima de 3× (mínimo 250), e sem contraste no histórico só os mínimos valem. Silêncio e ruído sem fala saem só no teto, sem multiplicar pedidos. Não há critério de "janela sem fala" que deixe de enviar: um falso negativo apagaria palavras, e a P124 só pula silêncio digital. Frames de 100 ms (`CaptureFrames`, `9886075`) e teto de 90 pedidos (`b4fbd5d`): ~3 min de fala, a captura muda para em ~6 min, e o custo máximo é de ~US$ 0,03 por sessão. `dictation_window` ganhou `cut` e `noiseFloor`. Latência esperada do fim da frase ao texto: ~1,7 s (pausa de 0,3 s + até 0,1 s de frame + ~1,3 s de resposta). Com uma frase curta no começo, o primeiro texto sai em ~2,5–3 s. **Falta com fala no S26:** o intervalo entre o fim da fala, `dictation_window` e `transcription_success`; `cut=pause` na maioria das janelas; `noiseFloor` em sala silenciosa e com ruído; a precisão com janelas curtas, incluindo ponto e maiúscula na emenda quando uma hesitação parte a frase; e a fila serial, visível quando `transcription_request` sai bem depois do `dictation_window` porque a janela anterior ainda está na OpenRouter. **Integrada e instalada no S26 (2026-09-15 18:50):** `audio capture started … readBufferBytes=3200, recordBufferBytes=12800` confirma os frames de 100 ms numa sessão de 1,6 s sem fala, cancelada sem `transcription_request` | /verificar (com fala) |
| P141 | Média | A bolha não volta sozinha depois de reinstalar ou atualizar o app: `adb install -r` mata o processo, o `FlowVoiceOverlayService` não é recriado e o interruptor "Botão flutuante" aparece desligado. Só volta pelo interruptor dos Ajustes ou pelo microfone do Início (visto no S26 em 2026-09-15, 0.5.0). O serviço é `foregroundServiceType=microphone`, e o Android 12+ restringe iniciar serviço em primeiro plano de microfone a partir do segundo plano, então religar pelo `onServiceConnected` da acessibilidade precisa de verificação | concluída (0.5.0): `BubblePositionStore` guarda "bolha ligada" (`bubble_enabled` em `flowvoice_overlay`, fora da sincronização), escrito quando o serviço sobe e apagado no `ACTION_STOP`. `OverlayAutoStart.shouldRestart` decide, e a `MainActivity.onStart` religa com o app em primeiro plano, onde não valem as restrições de serviço de microfone em segundo plano. **Validada no S26 (2026-09-15):** com a bolha ligada, `adb install -r` deixou a tela sem bolha (0 janelas) e abrir o app trouxe a bolha de volta (1 janela, posição padrão). Não resolve religar sem abrir o app, por exemplo depois de reiniciar o aparelho | /corrigir |
| P142 | Baixa (suspeito) | No Samsung Notes, cada trecho digitado no modo direto aparece como parágrafo próprio, embora o texto enviado comece com espaço e não com quebra de linha: `dictation_direct_inserted window=2 chars=53` para "E hoje o dia está muito bonito para ir para a praia." (52 caracteres + espaço). Visto no S26 em 2026-09-15, 0.5.0. Pode ser o editor do Samsung Notes tratando cada `commitText` como bloco novo, ou o teclado FUTO. Verificar com um app de campo simples (WhatsApp, Chrome) e com `insertFallback` antes de mudar algo | medida no S26 (2026-09-15, 0.5.0): **o parágrafo não vem do FlowVoice**. `DirectInsertionPlanner` (`:26`) separa com espaço, nunca com quebra de linha, e dois `commitText` seguidos pelo receiver de depuração, com 2 s e com 5 s de intervalo, entraram **na mesma linha** da nota. O parágrafo por trecho é do editor do Samsung Notes, e é cosmético. **Achado maior no mesmo teste:** com uma palavra em composição no teclado, o `commitText` a substitui e o texto sai truncado ("Primeiro trecho." virou "Primeiro"). A `AccessibilityService.InputMethod.AccessibilityInputConnection` não expõe `finishComposingText` (só `commitText`, `deleteSurroundingText`, `setSelection`, `sendKeyEvent`, `getSurroundingText`, `performEditorAction`), então não há como encerrar a composição por ali. Alternativas a medir: `setSelection` na posição atual antes de inserir, ou cair no `insertFallback`. Risco baixo no uso normal, porque o toque na bolha costuma vir depois de parar de digitar. **Pista decisiva da P148 (2026-09-16):** em dois ditados o campo tinha **exatamente um caractere a mais** do que a conta do app (`drift=1`), e a aritmética só admite uma origem — o apagar da P144 (`deleteSurroundingText(1,0)`), que o app desconta da conta mas o campo às vezes não executa. Note que isso **não** é o espaço sumido: espaço engolido deixaria o campo mais curto (`drift=-1`), são divergências de sinais opostos. E como no ditado das 11:05 houve **três** `erased=1` para um `drift` de 1, o apagar falha **às vezes**, não sempre — é condição intermitente, não incapacidade da API | concluída (sem aparelho): **o campo é conferido antes e depois de cada trecho**, em vez de se confiar na conta. Antes de apagar, o app confirma no campo que o caractere a tirar está mesmo lá (senão apaga zero e loga `dictation_erase_skipped motivo=nao_confirmado`); depois de escrever, relê e, se o fim do campo não for o que foi pedido, refaz o pedaço pela **rota atômica** `ACTION_SET_TEXT`, que troca num passo só, sem instante algum com o texto apagado (`dictation_write_mismatch ... acao=refeito`). Decisão pura em `DirectFieldWrite`; a chamada de API ficou fina. No Android, `insertDirect` passou a encerrar a composição com um `commitText("", 1, null)` antes do apagar — comprometer texto encerra a composição por contrato, e o commit vazio faz isso sem escrever nada. Sem leitura do campo (desktop, API < 33, campo ilegível) vale a conta do app, como antes; sem rota atômica, sem âncora ou com recusa das travas da P139, **nada é tocado** (`acao=sem_rota`, `sem_ancora`, `falhou`). **O que continua hipótese:** que a condição intermitente seja a composição do teclado — não dá para observar a composição pela API de acessibilidade. **Risco residual assumido:** se o `commitText("")` passar e o passo seguinte lançar exceção, a palavra em composição se perde; a janela de falha já existia entre apagar e escrever, e ficou um pouco mais larga. **Sinal de que a causa é outra:** `drift≠0` no fim **sem nenhum** `dictation_write_mismatch` durante o ditado — seria o teclado reagindo depois da nossa leitura, e aí a conferência passa para o começo do pedaço seguinte. **Falta com fala no S26** (roteiro: emenda com ponto no meio, emenda depois de nome próprio e ditado com palavra em composição) | /debugar |
| — | — | **Precisão da transcrição em português com janelas curtas (2026-09-16, 0.5.0):** P143. Medido no S26 pelo usuário (`openai/gpt-transcribe`, 2026-09-16 01:13); a correção foi feita sem aparelho e espera validação com fala | — | — |
| P143 | Alta | Depois da P140 as janelas passaram a sair na pausa da fala, com 1,4 a 4,2 s e resposta em ~1 s: a latência ficou boa, mas a **precisão em português piorou**. No ditado clínico do usuário saiu "Evoluiu posteriormente com episódios de **arreio**." (quase certamente "diarreia" partida entre janelas: "di" no fim de uma, "arreia" no começo da outra), "de **Grandmont**." (termo/nome errado sem contexto) e "de Grandmont.**Queda** da pressão arterial." (sem espaço entre dois trechos, embora `dictation_direct_inserted` mostrasse `chars` = tamanho do trecho + 1). Causa: cada janela é transcrita **sozinha**, sem nenhum áudio anterior, e quanto menor a janela menos contexto o modelo tem, com o erro concentrado nas bordas. A API de transcrição da OpenRouter não aceita `prompt` (só `language`, `temperature`, `response_format`, `timestamp_granularities` e `provider`), então não dá para passar contexto em texto | concluída (sem aparelho): **(1) contexto sobreposto em áudio** — cada janela vai com o último 1 s da anterior à frente (`SPEECH_CONTEXT_MS`), em `DictationWindow.contextPcm`, fora do `pcm`, para não mexer na linha do tempo, na contagem de bytes nem no silêncio digital (P124); o contexto acompanha o fluxo, então janela menor que 1 s ainda leva o resto da anterior, e a primeira janela não tem contexto. **(2) deduplicação endurecida** (`TranscriptOverlap`): comparação sem acento, sem pontuação e sem caixa, em três degraus — maior sufixo igual ao prefixo; o mesmo ignorando a última palavra de `left` quando ela é pedaço de palavra partida e a palavra inteira abre `right`, colando o que falta sem espaço; e o mesmo sem colar quando o pedaço não casa. Os degraus 2 e 3 exigem ao menos uma palavra de âncora, que só o contexto garante; pedaço de uma letra nunca cola e trecho só de pontuação não ancora. **(3) janela e pausa mínimas** de 1,2 s/300 ms para 2 s/450 ms. **Custo:** o áudio enviado por minuto sobe ~40 % (~24 janelas/min × 1 s), de ~US$ 0,0044 para ~US$ 0,0062 por minuto de ditado; `maxRequestsPerSession` fica em 90, que agora cobre ~3,7 min de fala. **Latência esperada:** fim da frase → texto ~1,95 s (era ~1,7 s): +150 ms da pausa maior, +0,1–0,2 s estimados pelo 1 s de áudio a mais por pedido; a primeira janela de uma frase curta espera até 800 ms a mais. **Riscos:** palavra repetida de propósito sem âncora ainda some ("é muito" + "muito importante"); o contexto pode voltar transcrito de outro jeito e escapar da deduplicação, dobrando palavra; e um pedaço de 2+ letras que seja palavra real pode colar na seguinte ("de" + "dengue"). **Sobre o espaço perdido em "Grandmont.Queda": não se reproduz em código puro** — o teste novo mostra que `DirectInsertionPlanner` emite o espaço, e o `chars=n+1` do log confirma que ele foi enviado ao inserir; a perda está no `commitText` ou no editor de destino, mesma família da P142. `separatorFor` passou a olhar a fronteira de verdade (pontuação que pertence à frase anterior entra colada), mas isso não cobre o caso medido. **Falta com fala no S26** (ver roteiro abaixo) | /corrigir |
| — | — | **P143 medida com fala no S26 (2026-09-16 01:56, 0.5.0 + P143, `openai/gpt-transcribe`, marcador da P135 ligado):** funcionou — `contextMs=1000` em todas as janelas menos a primeira; "diarreia", "hipertensão arterial sistêmica", "losartana", "desidratação" e "leucocitose" corretas; a hesitação não partiu a frase; e a deduplicação removeu o contexto repetido (janela com 62 caracteres no texto e `dictation_direct_inserted chars=54`; outra 35 → 26; outra 30 → 21). O teste expôs os achados P144 e P145 | — | — |
| P144 | Alta | **(1)** Janela sem fala vira lixo no campo: janelas cortadas como `cut=leading` (e o `flush` do fim) contêm só silêncio mais o contexto sobreposto da P143, e o modelo transcreve **o contexto**. No S26: `transcription_window_text window=3 text=3Gs.` (de "há três dias") → `dictation_direct_inserted window=4 chars=5`, e `window=16 text=Tô cansado.` repetindo o fim da frase anterior. Outras janelas `leading` voltaram com o contexto igual e a deduplicação as anulou (`text=de losartana.`, `text=de desidratação.`), mas ainda custaram uma requisição cada. **(2)** A emenda deixa ponto no meio da frase: o modelo fecha cada trecho com ponto e, quando o trecho seguinte continua a frase, sai "O exame de sangue. mostrou leucocitose." e "Estava muito. muito cansado." | concluída (sem aparelho): **(1)** `DictationWindow.voicedMs` mede a fala própria **só do `pcm`, sem o contexto**, com o limiar de fala do endpointer (relativo ao piso de ruído da sessão, nunca fixo, para fala baixa não virar silêncio — P124); a conta sai de `SpeechEndpointer.voicedBlocks`, extraído da mesma regra que já decidia o corte. `IncrementalTranscriptionController` pula a janela pelo caminho do silêncio digital: trecho `Ok` vazio, sem requisição, com a vaga do teto ocupada na submissão (P107). Só `leading` e `flush` são pulados — `pause` e `ceiling` sempre vão à API, senão quem fala baixo perderia o ditado inteiro; sem endpointing `voicedMs` é nulo e nada é pulado; janela sem fala também não leva mais contexto. `transcription_silent_window` ganhou `reason` (`digital`/`sem_fala`) e `cut`. **(2)** `DirectInsertionPiece.deleteBefore` manda apagar o ponto antes de escrever o pedaço, por `deleteSurroundingText` (e por `CursorInsertion.plan` no fallback). Só apaga com o texto do FlowVoice **logo antes do cursor** (`DirectInsertionPlan.contiguous`, que cai depois de recusa ou de "Inserir aqui"), só um ponto de trecho nosso, só quando o pedaço continua a frase (minúscula, pontuação da frase anterior ou palavra partida fechada), e só se a palavra antes do ponto for toda minúscula (senão "Dr." viraria "Dr"). A deleção roda depois das travas de destino e de senha. `dictation_direct_inserted` ganhou `erased`. **Maiúscula não é rebaixada, de propósito:** ela é o sinal de que o modelo encerrou a frase, e rebaixar corromperia nome próprio. **Riscos:** apagar caractere no campo do usuário (mitigado pelas travas acima); um ponto legítimo de abreviação toda minúscula ("etc.") seria apagado; com a sessão pausada e o pendente vazio o ponto não é tocado e o texto do plano fica um caractere à frente do campo. **Falta com fala no S26** (ver roteiro) | /corrigir |
| P145 | Baixa | Vocabulário do usuário não chega à transcrição: no S26 (2026-09-16) "dispneia" saiu "de Espinéia". A API de transcrição da OpenRouter não aceita `prompt` (P136/P143), então não dá para passar vocabulário ao modelo. O dicionário pessoal (`PersonalDictionary.apply`) já roda depois da transcrição, mas só troca termo por termo exato e não cobre erro fonético com fronteira de palavra trocada ("de Espinéia" = duas palavras). Proposta: lista de termos do usuário (clínicos e nomes próprios) aplicada depois da transcrição, com casamento aproximado sem acento e por fonema, cuidando para não reescrever fala legítima | concluída (sem aparelho): o `DictionaryApplier` deixou de ser troca de termo exato e passa a procurar o termo **por som**, em janelas de 1 a 4 palavras — é isso que atravessa a fronteira, nos dois sentidos ("de Espinéia" → "dispneia", "napraj" → "na praia"). A chave fonética (`PtBrSound`) tira acento, pontuação e espaço, faz `j`/`y` valerem `i` e colapsa letra repetida. A troca só acontece com **esqueleto de consoantes idêntico** (vogal o modelo troca o tempo todo; consoante nova é outra palavra), distância de edição ≤ 1, ou ≤ 2 a partir de 8 letras, e evidência de erro — fronteira mudou ou termo com 8+ letras. Barrado de propósito: `hipertensão` com "hipotensão" no dicionário (esqueleto `hprtns` × `hptns`, o `r` a mais salva o quadro clínico), `na praça` com "na praia", flexão (`comprimido` × `comprimida`), palavra sem consoante na borda ("o", "ao") e qualquer coisa com dígito. A correção entra **antes** de o trecho ir ao campo, no mesmo ponto onde o app conta o que escreveu, então a P148 e a revisão final continuam batendo caractere a caractere. Log `dictation_vocabulary_applied window= chars=`, sem texto. **Falta com fala no S26**: cadastrar os termos no Dicionário e ditar os quatro casos, inclusive a contraprova da hipertensão | /aprimorar |
| — | — | **P144 medida com fala no S26 (2026-09-16 09:12, 0.5.0 + P144, `openai/gpt-transcribe`, marcador da P135 ligado):** as duas correções funcionaram — texto de 45 caracteres inserido com 36, 43 → 30, 32 → 19, `erased=1` em três emendas e seis `transcription_silent_window reason=sem_fala`. Sobrou o caso da P146 | — | — |
| P146 | Alta | A deduplicação por texto não acha a repetição quando o modelo transcreve **o contexto de outro jeito**, e o trecho entra dobrado. No S26 (2026-09-16 09:12): janela 0 `text=Avaliado pelo doutor.` (inserido 21) e janela 1 `text=Segundo doutor Grandmont.` **inserida inteira, 26 caracteres, sem remoção**, porque "Avaliado pelo doutor" virou "Segundo doutor" na repetição do contexto. No campo: "Avaliado pelo doutor. Segundo doutor Grandmont." Comparar texto é frágil por construção: o contexto é o mesmo áudio, mas nada obriga o modelo a escrever as mesmas palavras | concluída (sem aparelho): **corte pelo tempo, não pelo texto**. A janela que leva contexto (P143) pede `response_format: "verbose_json"` com `timestamp_granularities: ["word"]`, e tudo o que **termina antes do fim do contexto** é descartado (`ContextTrim`), sem depender de o modelo escrever igual. A janela 0 não pede nada disso. **Degradação em cadeia:** com `words`, corte por palavra; só com `segments`, corte por segmento (mais grosso: um segmento costuma ser uma frase inteira e pode levar junto o começo da janela); sem tempos, ou com o provedor recusando o formato (HTTP 400/422), a janela é refeita em `json` na hora e vale a deduplicação por texto de sempre. A recusa fica lembrada por modelo (`transcription_verbose_unsupported reason=http_400|sem_tempos`), então é paga uma vez, não por janela. **Folga de 120 ms** (`ContextTrim.TOLERANCE_MS`), que anda para trás e nunca para a frente: na dúvida a palavra fica e a deduplicação por texto a remove — perder a primeira palavra da janela por arredondamento seria irreversível. O texto que sobra sai do `text` original (pontuação e caixa do modelo intactas) quando há uma palavra por token; se as contagens não baterem, é remontado das palavras. `transcription_context_trimmed window= strategy= contextMs= droppedMs= droppedWords= droppedSegments=`, sem o texto (esse só no log da P135). **Custo:** o áudio enviado não muda, só a resposta cresce (tempos de ~20 palavras por janela, ~1 KB); a recusa custa uma requisição HTTP extra por modelo, uma vez, e nenhuma vaga do teto da sessão (o teto conta janelas). **Latência:** o corte é local (uma passada nas palavras); o que pode pesar é o corpo maior na volta. **Riscos:** provedor que devolve tempo ruim ou sem `words` cai no corte por segmento, mais grosso; folga muito grande deixa palavra do contexto passar (a rede de texto pega), folga muito pequena arriscaria comer a primeira palavra da janela; e a resposta maior consome mais banda. **Falta com fala no S26** (ver roteiro) | /corrigir |
| P64 | Baixa | Remover `androidx.security:security-crypto` (e o tink-android que ele traz) e o código de migração do cofre antigo numa versão futura, quando nenhum aparelho tiver mais `flowvoice_secrets.xml` | pendente | /aprimorar |
| P65 | Média | Validar no S26: (1) migração da chave para `flowvoice_vault` (Ajustes mostra CONFIGURADA, logcat `legacy_vault_Migrated`, `flowvoice_secrets.xml` removido); (2) fallback `ACTION_SET_TEXT` e barra acima do teclado sem eventos de acessibilidade (SEG-5); (3) recusa em campo de senha e quando o foco muda de app (SEG-7) | parcial (2026-09-13, S26 0.4.1): (1) confirmada — `flowvoice_secrets.xml` removido e `flowvoice_vault.xml` com `openrouter_key_iv` (16) e `openrouter_key_ct` (120); serviço conecta sem tipos de evento (`Bound services`, `eventTypes=`, `flags=0x8041`), sem crash. Faltam (2) e (3) | usuário / /verificar |
| P67 | Bloqueante | Ditado de nota órfão: trocar de aba ou girar a tela durante o ditado recria o `NotesScreenState` (`remember` em `NotesRoute.kt:79`, sem `configChanges`). O microfone continua gravando e enviando áudio à OpenRouter sem nenhum caminho para parar: a bolha e o Início recusam com o pipeline ocupado, e ocultar o botão só cancela sessões `startedHere`. Se a troca ocorrer em "Transcrevendo", o texto ditado se perde | concluída (0.4.2, `765aebc`): `NoteDictationCoordinator` | /corrigir |
| P68 | Média | Testes de `androidApp` cobrem só lógica pura: sem teste de Compose nem Robolectric para posse da sessão nas Notas, `owned`/`dismissed` do overlay, `DictationStarter`, fluxos do `FlowVoiceRoot` e posicionamento da barra | pendente | /aprimorar |
| P69 | Média | TalkBack: toggles "Botão flutuante" e "Revisão por IA" sem nome (`SettingsRow` não mescla semântica, `FvToggle` sem rótulo); campos de título, corpo, novo termo e chave com placeholder solto em vez de rótulo | concluída (0.4.2, `bf6adf8`, `06ad03f`); ver P95/P96 (nome duplicado e `contentDescription` em campo editável) | /corrigir |
| P70 | Baixa | Waveform com a onda invertida: `StartOffsetType.FastForward` (`Waveform.kt:54`) adianta cada barra, e o CSS atrasa (`animation-delay`); usar `Delay` | pendente | /corrigir |
| P71 | Baixa | Barra 1b diverge do README sem registro no F12.5: sem "voltar ao app", rótulos de rota diferentes, cronômetro em peso 400 em vez de 500 | pendente | /corrigir |
| P72 | Baixa | Textos fixos que podem ser falsos: "Relatório exportado ✓" mesmo com o compartilhamento cancelado; "Rota principal commitText" fixo com minSdk 24; dicas fixas em Ajustes ("Rota commitText ativa", "Padrão escolhido no benchmark F05"); "CONFIGURADA" em vez de "VÁLIDA · hh:mm" | pendente | /corrigir |
| P73 | Baixa | Ditado na nota trata todo o texto ao vivo como provisório e não aplica o dicionário (`NotesRoute.kt:120-121`); o overlay já separa com `liveText()` | pendente | /corrigir |
| P74 | Baixa | Saídas de permissão: microfone negado de vez no onboarding não leva aos ajustes do app; o microfone do Início não confere a chave e só falha no fim | parcial: desde a 0.4.3 (P107) `start()` recusa sem chave e o Início avisa no início, não no fim (o aviso errado da segunda tentativa foi corrigido na 0.4.4, P110); resta o microfone negado de vez no onboarding | /corrigir |
| P75 | Baixa | Navegação: o passo "Chave" do onboarding abre Ajustes com a barra inferior, e tocar numa aba descarta o onboarding sem gravá-lo; "← ajustes" volta para o Início quando o Diagnóstico foi aberto pela pílula | pendente | /corrigir |
| P76 | Baixa | Custo: o overlay recompõe a cada 200 ms (o cronômetro muda a cada 1 s) e o serviço chama `getWindows()` na thread principal a cada 400 ms, inclusive em `Ready` sem limite | pendente | /aprimorar |
| P77 | Média (suspeito) | Barra do overlay provavelmente flutua acima do teclado com folga igual à barra de navegação: `y = altura da tela − topo do teclado` é aplicado num quadro que já exclui a navegação; o fallback dobra a folga (`FlowVoiceOverlayService.kt:225-250`) | pendente: confirmar no aparelho (P58) | /debugar |
| P78 | Baixa (suspeito) | Vão acima do teclado nas abas: `imePadding()` no conteúdo com a barra inferior como irmã na `Column` (`FlowVoiceRoot.kt`) | pendente: confirmar no aparelho | /debugar |
| P79 | Baixa (suspeito) | O anel de pulso da bolha (84 dp) deve sair cortado na janela `WRAP_CONTENT` (~76 dp) | pendente: confirmar no aparelho | /debugar |
| P91 | Bloqueante | Sessão iniciada pelo microfone do Início (`ACTION_START_DICTATION`, 22:29:58 no S26) ficou com o overlay em modo bolha: `dumpsys window` com `(45,360)(wrapxwrap) gr=BOTTOM END` e nenhuma barra na tela. O microfone gravou sem controle visível; a bolha ignora toques com o pipeline ocupado e só "Ocultar botão" na notificação para. `applyMode` não recria a view; causa ainda não confirmada (hipótese: `applyMode` grava `mode = Bar` antes de `updateLayout`, que sai sem aplicar se a view ainda não está anexada) | concluída (0.4.2, `6404e65`): causa confirmada (efeito de status inicial zerava `owned`); `OverlayOwnership` | /debugar → /corrigir |
| P92 | Alta | Teto de requisições por sessão (`DEFAULT_MAX_REQUESTS_PER_SESSION = 30`): depois de 30 janelas (~2 min) o `IncrementalTranscriptionController` só registra `transcription_budget` e a captura continua para sempre, sem aviso nem texto novo. No S26 foram mais de 101 janelas (~7 min) com o microfone aberto | concluída (0.4.2, `f23c303`) | /corrigir |
| P90 | Alta | Telas do sistema abertas na tarefa do FlowVoice: `startActivitySafely` e as chamadas diretas abrem `Settings.ACTION_*` sem `FLAG_ACTIVITY_NEW_TASK` (`ContextExt.kt:15`, `OnboardingRoute.kt:74`, `DiagnosticsRoute.kt:408`, `SettingsRoute.kt:570-574`, `HomeRoute.kt:147`). Depois de tocar em "Serviço de acessibilidade" no onboarding, o ícone do app reabre a tela de acessibilidade em vez do app. Evidência no S26: task 3008 com `baseActivity=MainActivity`, `topActivity=Settings$AccessibilitySettingsActivity`, `numActivities=2`, e o START do launcher com `result code=2` | concluída (0.4.2, `9b3284d`); validada no S26 em 2026-09-14 | /corrigir |
| P80 | Alta | SEG-7: na recusa (foco mudou de app ou campo de senha) a mensagem promete "texto mantido na barra", mas `insertReady` troca `Ready` por `Completed` com falha e o overlay esconde o resultado em 4 s, sem reinserir nem copiar: o texto ditado se perde. Manter `Ready` na recusa | concluída (0.4.2, `bc6e952`) | /corrigir |
| P81 | Média | SEG-5: sem tipos de evento, o cache de janelas do cliente de acessibilidade não é invalidado (`TYPE_WINDOWS_CHANGED` descartado com `mUsesAccessibilityCache=false`); a barra pode ficar presa na posição do teclado da primeira leitura. Correção candidata: `setCacheEnabled(true)` no `onServiceConnected` (API 33+) ou `clearCache()` antes de `windows` | concluída (0.4.2, `1281ef7`) só na API 33+; falta validar no aparelho (P94) | /corrigir |
| P82 | Média (suspeito) | SEG-5: pelo mesmo cache, o fallback `ACTION_SET_TEXT` pode ler texto antigo do campo e sobrescrever o que o usuário digitou depois (classe do P30). Mesma correção do P81 | provavelmente resolvida pela P81 na API 33+; abaixo disso o fallback segue exposto; validar (P94) | /debugar |
| P83 | Média | SEG-7: ditado iniciado pelo Início não tem conferência de destino (`captureTarget` roda com o FlowVoice em primeiro plano e grava `null`, que nunca bloqueia); o CHANGELOG 0.4.1 afirma que a inserção é recusada se o foco mudou. Recapturar no `finalizeForReview` e comparar no `insertReady` | concluída (0.4.2, `914ca5a`) | /corrigir |
| P84 | Média | O CI não roda `:androidApp:testDebugUnitTest`: `BackupRulesTest` e os demais testes de tela, componentes e overlay só rodam localmente | concluída (0.4.2, `851445a`); passo verde no CI | /corrigir |
| P85 | Baixa | SEG-7: a conferência compara pacote, não campo (mesmo app, outra conversa ou prontuário, passa) | pendente | /aprimorar |
| P86 | Baixa | SEG-7: o fallback detecta senha só por `node.isPassword` (transformação), deixando passar `VISIBLE_PASSWORD` e senha com "mostrar" ligado; nas APIs 26–32 o fallback é a única rota | pendente | /corrigir |
| P87 | Baixa | SEG-6: migração e leituras de chave rodam na thread principal (Koin `createdAtStart` → pipeline → `SecretStore`); com `Kept` a migração se repete a cada início de processo; cada `readOpenRouterKey` é uma operação de Keystore | pendente | /aprimorar |
| P88 | Baixa (suspeito) | SEG-6: uma migração que termina em `Kept`, seguida de uma chave B salva no cofre novo e de uma leitura transitória `null`, grava a chave antiga A por cima de B | pendente | /corrigir |
| P89 | Baixa (suspeito) | SEG-6: alias do Keystore existente mas inutilizável (`UnrecoverableKeyException`/`InvalidKeyException` persistentes) trava o cofre para sempre; só limpando os dados do app | pendente | /debugar |
| P66 | Alta | Instalado fora de loja (adb/APK), o Android 13+ bloqueia ligar o serviço de acessibilidade ("configurações restritas"): o interruptor não liga, só sobra o atalho, e o app fica sem inserção nem barra. Nem o onboarding nem o README orientam "Configurações → Aplicativos → FlowVoice → ⋮ → Permitir configurações restritas". Evidência no S26 (Android 16): `ACCESS_RESTRICTED_SETTINGS: default; rejectTime` logo após a tentativa, `initiatingPackageName=com.android.shell`, `enabled_accessibility_services=null` e o FlowVoice só em `accessibility_button_targets` | concluída (0.4.2, `56a2226`): orientação no onboarding e no README | /corrigir (onboarding + README) |
| P147 | Alta | **Pontuação do ditado direto totalmente discordante.** No S26 (2026-09-16 09:29, 0.5.0 + P146, `openai/gpt-transcribe`, marcador da P135 ligado): "as frases terminam com ponto final muito cedo, não há inserção de vírgula corretamente". O modo direto transcreve **janela a janela** (2–4 s, cortadas na pausa pela P140), e o modelo fecha cada janela com ponto e abre a seguinte com maiúscula, porque só enxerga aquele pedaço: `text=Hoje o dia está muito bonito.` → `text=Bonito. Por isso iremos para a praia.` → `text=Para a praia pela manhã.` → `text=A praia do futuro é a praia de escolha.` → `text=de escolha. Pois lá.` → `text=Pois lá tem uma água de coco bem gelada.`; também `text=Avaliado pelo doutor.` → `text=Pelo doutor Grandmont.` e `text=Estava muito.` → `text=Estava muito, muito cansado.`. As emendas (P143/P144) já tiram o contexto repetido e o ponto no meio da frase; o que falta é **pontuação de frase** — vírgula, ponto no lugar certo e maiúscula só onde deve | concluída (sem aparelho): **revisão final do ditado**, uma vez só, no fim. Ao encerrar o modo direto (toque na bolha ou teto), todo o texto que o próprio FlowVoice inseriu vai ao `ProofreadingClient` (`openai/gpt-4o-mini`), que só mexe em pontuação, maiúsculas, acentos e ortografia (prompt e `ProofreadingGuard` da P132, que barra troca de palavra, número, negação, aspas novas e marcas); aceito e diferente, o app **apaga exatamente os caracteres que inseriu** (`deleteSurroundingText`, como a P144) e escreve a versão revisada. **Por que no fim e não por emenda:** revisar o par (fim do anterior + trecho novo) a cada emenda custaria N chamadas em vez de 1, somaria ~1 s de latência **dentro** do ditado (hoje o usuário já vê o texto entrando) e ainda decidiria a pontuação sem conhecer o resto da frase — a vírgula de "…para a praia, pois lá tem água de coco" só é decidível depois do fim. Decisão pura em `DictationProofread` (`request`/`outcome`), sem rede nem campo. **Teto de 4000 caracteres** (~700 palavras, ~5 min de fala): acima disso não revisa, porque a troca é um apagar-e-reescrever do ditado inteiro num passo só, e revisar apenas o fim cortaria no meio de uma frase — justo a fronteira que a P147 existe para consertar. **Falhas (nada é apagado):** guard recusou, rede/chave, trecho pendente, texto não contíguo, ditado vazio, acima do teto, revisão idêntica; a contiguidade é conferida **de novo depois da resposta**, porque o campo pode mudar no ~1 s da chamada. **Custo:** ~US$ 0,0001 por ditado de 300 caracteres. **Latência:** ~1 s a mais no fim do ditado; `latencyMs` do `dictation_finalized` passa a incluí-la. **Prévia:** "revisando…" enquanto vai e volta. **Log:** `dictation_proofread_applied chars= erased=` e `dictation_proofread_skipped reason=`, sem texto (só no log da P135). **Riscos:** o texto "pisca" entre o apagar e o escrever (dois passos no `commitText`, imperceptível se der certo); se o `commitText` falhar **depois** do `deleteSurroundingText`, o ditado se perde (a rota `ACTION_SET_TEXT` é atômica e não corre esse risco); e o guard aceita uma pontuação ruim que não mude palavra. **Validada no S26 (2026-09-16 11:05)**: `dictation_proofread_applied chars=151 erased=151 drift=1`, com `proofreading_input text=...pela manhã, a praia do futuro é a praia de escolha pois lá tem uma água de coco bem gelada.` virando `...pela manhã. A Praia do Futuro é a praia de escolha, pois lá tem uma água de coco bem gelada.` no campo; a revisão levou ~2,9 s, dentro do teto de 5 s da P152 | /corrigir |
| P148 | Alta | **A revisão final apagou de menos e sobrou a primeira letra do ditado.** No S26 (2026-09-16 10:29, 0.5.0 + P147, Samsung Notes): `dictation_proofread_applied chars=151 erased=151` e o campo ficou com `HHoje o dia está muito bonito, por isso iremos para a praia pela manhã. A Praia do Futuro é a praia de escolha, pois lá tem uma água de coco bem gelada.` — a pontuação saiu certa, mas o campo tinha **152** caracteres e o app apagou os **151** que achava ter escrito. A conta do app (`progress.typed`) divergiu do campo em um caractere; a suspeita é o apagar da P144 (`dictation_direct_inserted window=2 chars=60 erased=1`, o ponto do meio da frase), que o editor pode não ter executado | concluída (sem aparelho): **o campo passa a ser a verdade**. Antes de trocar, a revisão final lê o texto que está de fato antes do cursor (`getSurroundingText` da `AccessibilityInputConnection`, API 33+) e casa com o ditado **letra a letra, ignorando espaços e pontuação** — que é justo o que diverge; o que sobra de sinais no meio entra no que será apagado (`DictationFieldTail.eraseLength`). Letra diferente no meio (alguém digitou junto) ou diferença acima de 16 caracteres para mais ou para menos: **não apaga nada**, `dictation_proofread_skipped reason=campo_diferente`. Quem não souber ler o campo continua com a conta do app. O log ganhou `drift=`, a diferença entre o campo e a conta: `drift=0` é o esperado, e o que não for zero diz que um apagar ou escrever anterior não saiu como o app anotou — é a medida que vai dizer se a P144 é mesmo a causa. **Validada no S26 (2026-09-16 11:05)**: `drift=1` — o campo tinha de novo um caractere a mais do que a conta do app (a conta dizia 150, o campo tinha 151), a troca apagou os 151 que estavam lá e nenhuma letra sobrou. O ditado teve três `erased=1` da P144, e a divergência foi de exatamente um caractere: a suspeita de que um desses apagares não chega ao campo do Samsung Notes segue de pé, agora sem consequência visível | /corrigir |
| P149 | Alta | **Uma palavra trocada custou a pontuação do ditado inteiro.** No S26 (2026-09-16 10:42, 0.5.0 + P148): `proofreading_input text=Hoje o dia está muito bonito. Tá muito bonito, por isso iremos para a praia pela manhã...` → `proofreading_output text=Hoje o dia está muito bonito. Está muito bonito, por isso...` → `dictation_proofread_skipped reason=guard`. A revisão trocou "Tá" por "Está" (palavra curta, que o `ProofreadingGuard` da P132 não deixa mudar — e com razão: é a mesma regra que barra "direito"→"esquerdo" e uma dose trocada), e o guard descartou **o texto inteiro**, junto com a vírgula e as maiúsculas que estavam certas. O campo ficou com a pontuação de janela: "Hoje o dia está muito bonito. Tá muito bonito, por isso iremos... A praia do Futuro é a praia de escolha, pois lá tem uma água. de coco bem gelada." | concluída (sem aparelho): **misturar em vez de vetar**. `ProofreadingMerge.merge` alinha ditado e revisão palavra a palavra: onde o guard aceita a palavra (acento, ortografia, maiúscula), entra a da revisão; onde não aceita, **fica a do ditado**, mas com a maiúscula que a revisão deu — depois de uma vírgula que virou ponto, a frase não recomeça com maiúscula. A pontuação e os espaços vêm sempre da revisão. Quantidade de palavras diferente não é revisão: não mistura nada. O resultado ainda passa pelo `ProofreadingGuard` inteiro, então aspas, dois-pontos, quebras de linha e marcas `<ditado>` continuam descartando. O texto misturado vai ao log da P135 (`proofreading_merged`). **Ainda não exercitada no S26**: no ditado de 2026-09-16 11:05 a revisão não trocou nenhuma palavra (nenhum `proofreading_merged` no log) e o guard aceitou o texto inteiro, então a mistura não precisou agir. Falta um ditado em que o modelo troque uma palavra curta, como o "Tá" por "Está" das 10:42 | /corrigir |
| P150 | Média | **Repetição na emenda quando o modelo transcreve o contexto com outras palavras.** No S26 (2026-09-16 10:42): janela 0 `text=Hoje o dia está muito bonito.` e janela 1 `text=Tá muito bonito, por isso iremos para a praia pela manhã.` — o contexto sobreposto (1 s) voltou como "Tá muito bonito" onde a janela anterior tinha "está muito bonito", e a deduplicação por texto da P143 não reconheceu, deixando "muito bonito" duas vezes no campo. A P146 (corte pelo tempo, que não depende das palavras) resolveria, mas está inerte: a OpenRouter recusa `verbose_json` para o `openai/gpt-transcribe`. A P149 também não resolve: tirar a repetição é remover palavras, que o guard não permite | concluída (sem aparelho): **degrau novo na emenda**, que só roda onde os degraus atuais do `TranscriptOverlap` desistiram (nada muda onde já se removia algo). Ele compara o fim do texto já emitido com o começo do trecho novo deixando as palavras divergirem um pouco (`EditDistance`, extraída do `ProofreadingGuard` e agora compartilhada), cercado de quatro lados: **(1)** só age se a janela veio com contexto (`contextDurationMs > 0`), que passou a ser levado até a emenda; **(2)** teto em caracteres vindo do tempo do contexto (30 por segundo, o dobro da fala corrida, máximo de 120) — com 1 s cabem 30, e "Tá muito bonito," tem 16; **(3)** só a emenda, sufixo contra prefixo, nunca o miolo, então repetição que o usuário fez de propósito está fora de alcance por construção; **(4)** exige evidência — duas palavras casando ou uma de 8+ letras, e palavra curta divergente ("tá" por "está") só vale encostada em vizinhas idênticas. Não remove: sem contexto, contexto curto demais (200 ms), evidência fraca (`"Hoje o dia está"` + `"Tá chovendo muito."` fica inteiro) e repetição no meio da fala. **validada no S26 (2026-09-16 11:05)**: `transcription_window_text window=1 text=Por isso iremos para a praia pela manhã, a praia do futuro.` (59 caracteres) entrou no campo como `dictation_direct_inserted window=2 chars=39 erased=1` — o "Por isso iremos para " repetido do contexto saiu, e o campo ficou sem nenhuma palavra dobrada. As janelas 2 e 3 também emendaram limpo (31→22 e 58→41) | /corrigir |
| P151 | Média | **Silêncio no fim do ditado custa requisições.** No S26 (2026-09-16 10:42), entre o fim da fala (10:42:26) e o toque que encerrou (10:42:54), três janelas foram transcritas e voltaram vazias (`transcription_success chars=0`, janelas 6, 7 e 8), enquanto outras do mesmo silêncio foram barradas pelo `SilentWindow`/`onlyContext` (`reason=sem_fala`). Com o piso de ruído alto (`noiseFloor=87`), a detecção de silêncio deixa passar janelas sem fala | concluída (sem aparelho): **janela sem fala medida não vai à rede, em nenhum corte**. A causa não era o `SilentWindow`: o `onlyContext` da P144 só barrava os cortes `leading` e `flush`, e com ruído de sala parado nenhum bloco cai abaixo do nível de pausa — não há pausa, não há corte `leading`, e a janela sai **no teto**, indo sempre à API. Reproduzido sem aparelho: 4,4 s de ruído parado no agregador real dão `cut=ceiling durationMs=3160 voicedMs=0`, 20 ms da janela medida no S26. O `WindowSpeechGate` barra a janela com menos de **80 ms** de voz medida — o menor limiar acima de zero que existe na escala, já que `voicedMs` só conta trechos de 80 ms ou mais (`voicedMs ∈ {0, 80, 100, …}`), e o primeiro valor que passaria a barrar fala a mais seria 100 ms; 400 ms de fala real dão cinco vezes a barreira. `voicedMs` nulo (sem endpointing) é "não medido" e vai à API: falta de medição nunca vira silêncio. A janela barrada **continua ocupando a vaga do teto** (P107). No ditado das 10:42 teria economizado as 3 requisições, ~14 s de áudio enviado. Log: `transcription_silent_window reason=fala_insuficiente cut=ceiling voicedMs=0`, e o `dictation_window` ganhou `voicedMs=`. **validada no S26 (2026-09-16 11:04) com o sussurro primeiro**, como manda a ressalva: "Exame de sangue mostrou leucocitose." dito bem baixo saiu inteiro, com `dictation_window window=1 cut=pause noiseFloor=17 voicedMs=960` — doze vezes o limiar de 80 ms, e nenhum `reason=fala_insuficiente` em nenhum dos dois ditados. O ditado normal seguinte teve todas as janelas de fala entre `voicedMs=840` e `2580`, e as três janelas mudas do fim (`voicedMs=0`) foram barradas pelas travas antigas (`reason=sem_fala`, cortes `leading` e `flush`). O caso que a P151 veio pegar — janela de teto com `voicedMs=0` — não se repetiu nestes ditados, então a trava nova ainda não precisou agir: ela continua sem prova de fogo no aparelho. **Prova de fogo feita no S26 (2026-09-16 11:50)**, no ditado sussurrado com 20 s de silêncio antes de encerrar: as oito janelas do silêncio foram barradas e **nenhuma requisição foi gasta à toa** — cinco pelas travas antigas (`reason=sem_fala`, cortes `leading` e `flush`) e **três pela trava nova** (`reason=fala_insuficiente cut=ceiling voicedMs=0`, janelas de 3840, 3480 e 3520 ms), exatamente o caso que a P151 veio pegar. O sussurro passou inteiro: `voicedMs=220 peak=491` e `voicedMs=420 peak=704`, contra `peak` de 100 a 341 nas janelas mudas | /corrigir |
| P152 | Média | **A revisão final podia segurar o fim do ditado por até 30 s.** A chamada da P147 não tinha teto próprio: valia o `requestTimeoutMs` do `OpenRouterConfig` (30 s). Com a rede pendurada, o ditado já estaria no campo e o usuário ficaria parado esperando o texto trocar, sem saber que não vai trocar. Medido no S26: as duas revisões reais levaram ~1,5 s (2026-09-16 09:29 e 10:42) | concluída (sem aparelho): teto de **5 s** só para a revisão final (`DictationProofread.TIMEOUT_MS`, três vezes a folga do medido), com `withTimeoutOrNull`. Estourou, o ditado fica exatamente como foi digitado e o log registra `dictation_proofread_skipped reason=demorou`. O teto vale só para a revisão do fim; a transcrição continua com o teto de 30 s | /corrigir |
| P153 | Média | **Janela com voz medida, mas transcrição vazia, ainda custa requisição.** No S26 (2026-09-16 11:04), no ditado sussurrado: `dictation_window window=2 durationMs=3280 cut=ceiling noiseFloor=16 voicedMs=840` → `transcription_success window=1 chars=0`, e `window=3 durationMs=1580 cut=pause voicedMs=800` → `chars=0`. Duas chamadas pagas sem uma palavra, com a fala já transcrita na janela anterior. A trava da P151 não pega esses casos, e com razão: elas têm voz medida bem acima dos 80 ms. **A primeira hipótese estava errada e foi descartada lendo o código**: o `voicedMs` é medido só no `pcm` próprio da janela, nunca no contexto (`DictationWindow`, comentário da P144), então não havia contexto sendo contado como fala. A causa real é o limiar de fala do endpointer: `max(minSpeechLevel, speechFactor × piso)` = `max(250, 3 × 16)` = **250** num quarto silencioso, e 250 é ~0,8 % da escala de 16 bits (−42 dBFS) — respiração e ruído de sala cruzam isso por mais de 80 ms sem esforço. Só o modelo sabe que ali não havia palavra | concluída (sem aparelho): **a sessão aprende com a resposta que já foi paga**, em vez de eu adivinhar um limiar novo — que é justamente como se quebra quem fala baixo (P124). Cada janela passou a levar o `peakLevel`, o bloco mais alto do seu áudio próprio, na mesma escala do piso de ruído. O `EmptyAudioMemory` guarda dois números da sessão: o **maior pico entre as janelas que voltaram vazias** e o **menor pico entre as que renderam texto**. A janela é pulada quando não é mais alta que alguma que já voltou vazia **e** é mais baixa que qualquer uma que já rendeu texto — `transcription_silent_window reason=nivel_ja_vazio`, ocupando a vaga do teto como as demais (P107). Sem pico medido (sem endpointing) nada é pulado, e janela vazia sem pico não ensina nada. No ditado sussurrado das 11:04 teria economizado a segunda das duas chamadas vazias; no ditado das 10:42, duas das três. O `dictation_window` ganhou `peak=`, que é o número para conferir a separação entre sussurro e respiração no aparelho. **Ainda não exercitada no S26**: no ditado sussurrado de 2026-09-16 11:50, com 20 s de silêncio, a P151 barrou as oito janelas mudas antes que qualquer uma fosse enviada, então nenhuma voltou vazia e não houve o que a P153 aprendesse (nenhum `reason=nivel_ja_vazio` no log) — ela é a rede de baixo, para quando o silêncio passa pela trava de cima, que foi o caso das 11:04. O que o ditado deu foi a **separação de níveis** que faltava: sussurro em `peak=491` e `704`, silêncio entre `peak=100` e `341`. A margem é estreita (o silêncio mais alto ficou a 30 % do sussurro mais baixo), o que confirma que fixar um limiar de nível seria arriscado e que aprender com a resposta já paga é o caminho certo | /corrigir |
| — | — | **P146 sem efeito com o modelo atual (S26, 2026-09-16 09:29):** `transcription_verbose_unsupported window=1 model=openai/gpt-transcribe reason=http_400 status=400` — a OpenRouter **recusa `verbose_json`** para o `openai/gpt-transcribe`, então o corte do contexto pelo tempo nunca roda e vale sempre o plano B (deduplicação por texto da P143), que é exatamente o que a P146 veio substituir. A degradação em cadeia funcionou (o ditado não parou) e a recusa foi paga uma vez, como projetado. Sugestões, **não implementadas**: (1) fixar `provider` na chamada de transcrição, já que o formato depende do provedor que a OpenRouter sortear, e medir se algum aceita `verbose_json` com `timestamp_granularities`; (2) avaliar um modelo que aceite tempos (`openai/gpt-4o-mini-transcribe`, `deepgram/nova-3`) contra o WER do `gpt-transcribe` no Benchmark F05, porque trocar o modelo por causa dos tempos pode custar mais em WER do que a P146 economiza | pendente | /debugar |
