# F01 — Scaffold Kotlin Multiplatform + Gradle

**Status:** [x] concluída

Estabelecer o projeto KMP (shared comum + Android + desktop) com a stack fixada
no plano, Ktor/Koin já integrados e build verificável.

## Sub-tarefas

- [x] **F01.1 — Estrutura Gradle e versões**
  - `settings.gradle.kts`, `gradle.properties`, `build.gradle.kts` raiz e wrapper.
  - `gradle/libs.versions.toml` com versões fixadas: AGP 8.10.1, Kotlin 2.1.10,
    Compose BOM 2025.06.00, Ktor 2.3.13, Koin 3.5.6, kotlinx-serialization 1.7.3.
  - **Aceite:** configuração do projeto resolve sem erros (`./gradlew help`).

- [x] **F01.2 — Módulo `shared` (KMP)**
  - Alvos: Android (armeabi-v7a/arm64-v8a) + desktop (JVM 17).
  - `commonMain`: `platformName` (expect), `Version`, Ktor com
    `ContentNegotiation` + `json` (`HttpClientConfig<*>.applySharedConfig()`),
    `OpenRouterApi` + `OpenRouterApiClient` (`listTranscriptionModels`),
    `AppModule` Koin (definitions clássicas `single`).
  - `actual` do client por plataforma: `OkHttp` (Android) e `Java` (desktop),
    ambos no pacote `...shared.http`.
  - **Aceite:** `:shared:compileDebugKotlinAndroid` e `:shared:desktopTest` verdes.

- [x] **F01.3 — Módulo `androidApp`**
  - AGP 8.10.1 com compileSdk/targetSdk 35, toolchain 17; `FlowVoiceApp` + `MainActivity` (placeholder
    Compose) apontando para o `shared`.
  - **Aceite:** `:androidApp:assembleDebug` verde e APK em
    `androidApp/build/outputs/apk/debug/`.

- [x] **F01.4 — CI**
  - `.github/workflows/android.yml`: `:androidApp:assembleDebug`,
    `:shared:desktopTest` e upload do APK em `main`.
  - **Aceite:** comandos do pipeline passam localmente com o mesmo JDK 17.

## Critério de aceite da fase

- `assembleDebug` + `desktopTest` verdes na ordem do CI (JDK 17 + Android SDK).
- KMP com targets Android/desktop compilando; Ktor e Koin funcionando em
  `commonMain` sem hacks de classe por plataforma.
- Versões fixadas em `libs.versions.toml` (sem `latest`).