# F05 — Benchmark de modelos de transcrição (aparelho real)

**Status:** [x] concluído (rodada 1 medida, padrão + fallback decididos)

Medir a shortlist do PLAN com corpus pt-BR no S26 Ultra: **latência**,
**precisão (WER)** e **custo**. Escolher modelo padrão + fallback por número,
não por suposição. Requer **limite de gasto** definido pelo usuário.

## Corpus

- Principal: **curtos de 11/set** (`/Users/rafaelbrauner/.claude/jobs/f52a0fd2/tmp/curtos/`):
  150 itens com gabarito (`comando` 64, `frase` 77, `fantasma` 8, `ilha` 1),
  5,9 min de áudio. Cobre comandos de pontuação, frases, fantasmas e a ilha
  da pausa real ("E aí"). Local de referência: `resultados.jsonl` (config `app`).
- Complementar: **trechos reais** (`.../tmp/sinais/reais/`, 79 itens, 4,1 min):
  mede fantasma em pausas reais do aparelho.
- Gravações 11/set (`build/voz/gravacoes-11set/`, 7,6 min) indisponíveis neste
  checkout — recriar via `preparar_ditado_longo.py --puxar` se preciso.

## Sub-tarefas

- [x] **F05.1 — Protocolo + harness**
  - Ferramenta: `intelligent-keyboard/tools/voz/nuvem.py` (somente leitura,
    fora deste repo): `rodar` (JSON `input_audio` base64, `language=pt`,
    `temperature=0`, `response_format=verbose_json`, retry só 429/5xx,
    para em 401/402/403, JSONL com retomada + `limite_usd`), `resumo`
    (WER via `normaliza`, mediana por frase, perfeitas, comandos, fantasmas,
    p50/p95, custo total e por minuto).
  - `--seco` nos curtos: 150 itens, 5,9 min, US$ 0,0012 (deepinfra) a
    US$ 0,0040 (groq) por modelo. 8 modelos da rodada 1 ≈ US$ 0,01–0,03.
  - Métricas por modelo: WER geral + por categoria, mediana, perfeitas,
    comandos certos, fantasmas, p50/p95 latência, custo.
- [x] **F05.2 — Rodada 1 (8 modelos, PLAN)**
  - Corpus curtos 11/set, 150 itens, 5,9 min, `language=pt`, `temperature=0`.
  - Resultado (detalhe em `docs/benchmarks/F05-rodada1.md`; JSONs brutos
    arquivados em `~/.config/FlowVoice/benchmarks/`):
    `mai-transcribe-2` 19,3% > `gpt-transcribe` 19,8% > `turbo` 23,0% >
    `voxtral` 28,8% > `parakeet`/`gpt-4o-transcribe` 35,4% >
    `gpt-4o-mini` 42,4% > `nova-3` 52,3%.
  - Trio `gpt-*-transcribe` não aceita `verbose_json` (400) — medido com
    `response_format=json` direto, sem sinais/segmentos.
  - Custo total ~US$ 0,12, dentro do limite US$ 1,00 aprovado.
  - **Aceite:** tabela WER/latência/custo dos 8 publicada.
  - `openai/whisper-large-v3-turbo` (baseline), `openai/gpt-4o-mini-transcribe`,
    `openai/gpt-transcribe`, `openai/gpt-4o-transcribe`, `deepgram/nova-3`,
    `microsoft/mai-transcribe-2`, `nvidia/parakeet-tdt-0.6b-v3`,
    `mistralai/voxtral-mini-transcribe`.
  - Comandos (no repo intelligent-keyboard, chave em `~/.config/openrouter.key`):
    ```bash
    python3 tools/voz/nuvem.py rodar /Users/rafaelbrauner/.claude/jobs/f52a0fd2/tmp/curtos \
      --modelo <id> --saida <nome> --limite-usd 1.0
    python3 tools/voz/nuvem.py resumo <resultado.json> \
      --local /Users/rafaelbrauner/.claude/jobs/f52a0fd2/tmp/curtos/resultados.jsonl --config-local app
    ```
  - **Aceite:** tabela WER/latência/custo dos 8 + comparação com local `app`.
- [x] **F05.3 — Rodada 2 (conforme resultado)**
  - Dispensada: `nemotron/qwen3-asr/chirp-3` não prometem cobrir a combinação
    WER+fantasma do `mai-transcribe-2`; reavaliar se preço/latência pesar no
    uso real.
  - `nvidia/nemotron-3.5-asr-streaming-multilingual-0.6b`, `qwen/qwen3-asr-0.6b`,
    `qwen/qwen3-asr-1.7b`, `google/chirp-3` — só os que prometem algo que a
    rodada 1 não entregou.
- [x] **F05.4 — Decisão + registro**
  - **Padrão:** `microsoft/mai-transcribe-2` (19,3% WER, 108 perfeitas,
    0 fantasmas, p50 736ms). **Fallback:** `openai/whisper-large-v3-turbo`
    (23,0%, 10× mais barato, E2E validado F04.7).
  - PLAN atualizado (shortlist com números); seletor da UI já lista os 21
    modelos de transcrição via `listTranscriptionModels`.
  - Limite de gasto: rodada 1 ~US$ 0,12 de US$ 1,00 aprovado.
  - Modelo padrão + fallback registrados no PLAN com números; seletor da UI
    (`ModelStore`) atualizado; limite de gasto documentado.

## Limite de gasto (a confirmar pelo usuário)

- Estimativa rodada 1: **US$ 0,01–0,03** (8 modelos × 5,9 min).
- Proposta: `--limite-usd 1.0` por rodada (trava técnica, gasto real ~3% disso).
- Sem limite confirmado, nenhuma chamada real é feita.

## Critério de aceite da fase

- Tabela comparativa rodada 1 publicada em `docs/benchmarks/`.
- Modelo padrão + fallback escolhidos por número e registrados no PLAN.
- Custo total dentro do limite; nenhum segredo commitado.
